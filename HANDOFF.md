# WaterX 开发交接文档

> 更新时间：2026-08-29（Asia/Shanghai）
> 文档用途：让一个完全不了解背景的新会话，在不依赖长聊天记录的情况下，安全地接续 WaterX 开发。
> 核实依据：当前本地代码、目录结构、`package.json`、后端 `pom.xml`、Flyway 迁移、Git 分支与工作区状态；不是仅凭聊天记忆整理。
> 安全说明：本文不包含账号密码、CDS Project Key、API Key 或其他敏感凭据。

## 1. 新会话首先执行

新会话建议第一句话：

```text
请先读取项目根目录 HANDOFF.md，再核对实际代码和 Git 状态；本阶段仅修改本地并预览，不推送 GitHub、不部署 CDS。不要覆盖或丢弃现有未提交修改。
```

接手后先做只读检查：

```bash
cd /Users/fen/Documents/0-AIX/WaterX工艺管理
pwd
git status --short --branch
git log -1 --oneline --decorate
```

在理解工作区之前，不要执行 `git reset --hard`、`git checkout -- <file>`、清理未跟踪文件或大范围格式化。

## 2. 项目目标

WaterX 的产品定位已经从最初的“市政污水处理厂安全生产管理系统”扩展为“智慧水务运营平台”。目标是面向多污水处理厂、多工艺线和不同岗位，逐步统一承载：

- 生产运行与工艺诊断；
- 班组排班、交接班、当班任务和运行日志；
- 化验原始记录与化验报表；
- 设备管理；
- 安全生产管理；
- 库存管理与提质增效；
- 经营管理、过程评价、管理质量和改进提升；
- 基础资料、组织、人员与权限；
- 管理驾驶舱和个人事项。

当前产品仍处于“可交互业务原型逐步工程化”的阶段：安全模块后端化程度较高，生产运行和化验模块主要在管理端前端实现，部分数据仍使用浏览器 `localStorage`。

## 3. 真实项目目录与仓库

- **真实项目目录**：`/Users/fen/Documents/0-AIX/WaterX工艺管理`
- GitHub 仓库：`https://github.com/stone6484/WaterX.git`
- 当前分支：`codex/release-waterx-20260816`
- 当前提交：`5082664 优化智慧水务生产运行与化验管理`
- 当前提交与远端 `origin/codex/release-waterx-20260816` 一致。

### 当前未提交修改

截至本文生成时，以下文件存在本地修改：

```text
M apps/admin-web/package.json
M apps/admin-web/src/App.vue
M apps/admin-web/src/diagnosis.css
M apps/admin-web/src/main.ts
M apps/admin-web/src/risk.css
M pnpm-lock.yaml
?? apps/admin-web/src/module-shell.css
?? apps/admin-web/src/modules/management-quality/
?? HANDOFF.md
```

这些修改是当前阶段的有效成果，不是可随意丢弃的临时垃圾。大致包括：

- 安全管理 UI 向 WaterX 蓝色统一视觉迁移；
- 工况起止日期与自动匹配；
- 运行数据填报汇总、Excel 导入导出和分页；
- 工艺诊断“更新/保存”及工艺分析日报生成；
- 新增前端 `xlsx` 依赖。
- 重排一级导航并新增生产运行、库存、过程评价、管理质量、改进提升页面骨架。

**当前用户要求：只修改本地并本地预览，暂不提交、推送 GitHub，也不部署 CDS。**

## 4. 技术结构

### 4.1 总体形态

项目采用 pnpm workspace + 前后端分离，后端按照模块化单体思路组织。

```text
WaterX工艺管理/
├── apps/
│   ├── admin-web/       Vue 3 + TypeScript + Vite 管理端
│   └── mobile-h5/       Vue 3 + TypeScript + Vite 手机 H5
├── packages/
│   └── api-client/      前端共享 API 客户端与类型
├── backend/             Java 21 + Spring Boot 3.4.7
├── deploy/
│   └── docker-compose/  PostgreSQL / Redis / MinIO 等本地基础设施
├── docs/
│   └── adr/             架构决策记录
├── package.json
└── pnpm-lock.yaml
```

### 4.2 前端

- Vue `3.5.17`
- TypeScript `5.8.3`
- Vite `7.0.5`
- pnpm `11.9.0`
- Excel 导入导出：`xlsx 0.18.5`

管理端当前大量业务仍集中在 `apps/admin-web/src/App.vue`。它已经很大，继续增加功能前应逐步拆为页面组件、组合式函数和服务层，避免单文件继续膨胀。

### 4.3 后端

- Java 21
- Spring Boot 3.4.7
- Spring Web / Security / Validation / JDBC / Actuator
- PostgreSQL
- Flyway 数据库迁移，目前有 `V1` 至 `V18`
- Springdoc OpenAPI
- 本地预览可通过 Zonky Embedded PostgreSQL 启动临时数据库

### 4.4 架构和隔离原则

- 多租户、多水厂；业务数据必须带租户/水厂数据域。
- 生产运行后续还必须增加“工艺线”维度。
- 安全领域后端采用模块化单体，相关决策见 `docs/adr/`。
- 登录会话为不透明会话令牌方案，不应在前端自行推断权限。

## 5. 当前导航与模块状态

| 一级模块 | 二级模块 | 当前状态 |
|---|---|---|
| 首页 | 管理驾驶舱、个人事项示例 | 已有可视原型，数据以示例为主 |
| 生产运行 | 班组与排班、交接班管理、当班任务、运行日志 | 已有可点击页面骨架，业务规则待专项设计 |
| 工艺管理 | 工艺设计标准 | 已有本地交互原型；由原“生产运行”模块原样更名迁入 |
| 工艺管理 | 工况矩阵管理 | 已有本地交互原型，支持起止日期和日期匹配 |
| 工艺管理 | 运行数据填报 | 已有日报列表、填报、锁定、批量导入导出、分页 |
| 工艺管理 | 工艺诊断分析 | 已有结果指标/关键控制分析、状态计数、配置弹窗、保存日报 |
| 工艺管理 | 工艺分析日报 | 已有由诊断保存生成的汇总列表 |
| 工艺管理 | 工艺调整记录 | 规划中 |
| 化验管理 | 原始记录管理 | 已有 COD、氨氮、SS、粪大肠菌群四类原型 |
| 化验管理 | 化验报表管理 | 已有化验日报原型，月报等未做 |
| 设备管理 | 设备台账、维护保养 | 导航规划中 |
| 安全管理 | 安全态势及各安全业务 | 核心业务已实现，近期正统一 UI |
| 库存管理 | 库存总览、物资台账、入库管理、出库与领用、库存盘点、库存预警 | 已有可点击页面骨架 |
| 经营管理 | 经营目标、生产计划、预算管理、执行分析、成本收益、回款管理 | 已有可点击页面骨架 |
| 提质增效 | 能耗计量、能效分析 | 由“节能降耗”更名，原规划内容不变 |
| 过程评价 | 运行管理评价、设备管理评价、化验管理评价、安全管理评价、综合管理评价、问题与整改、评价报告 | 已实现五模块131项完整链路；独立评价总览已取消 |
| 管理质量 | 合法合规、稳定达标、安全运行、经济高效 | 已实现18项、100分讨论稿原型；总览入口已取消，未固化正式评分公式 |
| 改进提升 | 问题清单、改进计划、整改执行、复核关闭、改进分析 | 已有可点击页面骨架 |
| 基础信息 | 通用基础资料 | 规划中；原独立“指标管理/指标配置”入口已取消 |

当前一级菜单固定顺序：

```text
首页 → 生产运行 → 工艺管理 → 设备管理 → 化验管理 → 安全管理 → 库存管理 → 经营管理 → 提质增效 → 过程评价 → 管理质量 → 改进提升 → 基础信息
```

产品入口保持专业模块独立，不为形式上的 PDCA 合并一级模块。业务关系仅用于说明管理闭环：经营目标/生产计划属于 P；生产、工艺、设备、化验、安全、库存、提质增效等属于 D；过程评价和管理质量属于 C；改进提升属于 A。

## 6. 已完成的重要功能

### 6.1 平台框架和视觉

- 品牌名称调整为“WaterX 智慧水务运营平台”。
- 顶部全宽深蓝栏，Water 使用白色、X 使用橙色作为点睛色。
- 左侧导航可折叠，折叠后保留窄图标栏，内容区自动扩展。
- 业务图标按污水行业模块语义设计，保持简约和同一线性风格。
- 页面整体采用蓝色、浅灰、白色为主，强调紧凑、清晰、严谨。
- 登录页已移除“多厂协同 / 数据驱动 / 闭环运营”三个标签。
- 安全管理近期已从大量绿色按钮/卡片向 WaterX 蓝色和中性色统一。

### 6.2 安全管理

安全管理是最早建设、后端完成度最高的业务域，已有：

- 多租户、多水厂、登录、刷新、退出和首次修改密码；
- 组织架构、人员档案和厂区区域；
- 风险辨识，LS/LEC 评估、四色分级、审批、复评和风险告知；
- 检查计划、检查模板、检查任务；
- 隐患上报、整改、验收、附件、逾期提醒和闭环；
- 十类危险作业票、审批、现场执行控制和关闭；
- 培训材料、培训任务、考试和人员资质到期提醒；
- 安全承诺书与访客安全告知/登记；
- 特种设备、安全附件、消防和应急物资台账；
- 职业危害因素、职业健康体检；
- 安全预算和费用登记；
- 手机 H5 的风险告知、检查、隐患、作业票、培训和承诺相关页面。

注意：本轮只调整安全模块 UI，不应改变其既有业务流程和 API 行为。

### 6.3 工艺设计标准

- 按水厂、工艺线维护建设或改扩建设计文件中的固定设计值。
- 设计值作为工况目标和诊断分析的基准，不随日常填报频繁变化。
- 页面自身带齿轮配置入口；中央统一“指标管理”模块已按用户决策取消。
- 不应展示对设计阶段无意义的派生指标，例如仅靠实际进出水计算的去除率。

### 6.4 工况矩阵管理

- 一个工况代表一组指标目标值，体现“一厂一策”。
- 支持多个工况，如夏季、冬季、雨季、高负荷等。
- 工况字段：名称、起始日期、结束日期、说明、目标值集合。
- 已从单一实施日期改为起止日期。
- 已加入起止日期合法性和工况日期重叠校验。
- 运行填报/诊断分析按照业务日期自动匹配包含该日期的工况。
- 没有匹配时明确显示“未匹配工况”，不应静默套用错误目标。

### 6.5 运行数据填报

- 每天、每水厂、每工艺线形成一条运行日报。
- 列表支持新建、编辑、删除、锁定、刷新等顶部操作。
- 行尾重复的“编辑/删除”按钮已取消，以顶部选中后操作为准。
- 汇总表横向展示当前所有需要填报的一手字段。
- 已取消“进水 COD 最大值/最小值”查询输入框。
- 查询、清空之后增加“批量导入”和“批量导出”。
- 批量导出将当前汇总数据输出为 Excel。
- 批量导入从 Excel 读取数据，并处理格式错误、锁定记录和日期工况匹配。
- 底部有总数、每页条数、页码、前后翻页和跳页分页器。
- 填报页按指标分类分页签，顺序与工艺诊断分析一致。
- 只呈现需要人工填报的一手数据；由公式计算的指标不增加填报负担。

### 6.6 工艺诊断分析

- 按水厂、工艺线、分析日期诊断。
- 工况由分析日期自动匹配。
- 顶部“更新分析”已改名为“更新”，后面增加“保存”。
- 结果指标页签：进水水质、进水特征、出水水质、处理效能、污泥性状、沿程分析。
- 关键控制页签：水量控制、曝气控制、回流控制、排泥控制、加药控制、搅拌控制。
- 每个页签显示正常、预警、告警三个带圈计数；非零为填充圈，零为空心圈。
- 页签带齿轮配置入口，配置使用模态弹窗，打开时锁定背景页面。
- 指标表保留：指标、单位、设计值、目标值、实际值、偏差与状态、指标意义。
- 设计值、目标值、实际值字号一致；实际值可加粗。
- 列宽以内容完整且尽量紧凑为原则；指标意义不换行，必要时才允许横向滚动。
- 已取消各页面序号、指标编号和最大化按钮。

### 6.7 工艺分析日报

- 工艺诊断点击“保存”后，按水厂 + 工艺线 + 分析日期生成或更新一条日报记录。
- 列表只显示：水厂、工艺线、匹配工况、分析日期、正常指标个数、预警指标个数、告警指标个数、更新时间。
- 日报不是独立填报表，不提供重复的“更新/保存”表单。
- 当前记录保存在浏览器 `localStorage`，键名为 `waterx-process-analysis-reports`。

### 6.8 化验原始记录和报表

已实现四类原始记录原型：

- COD 容量法原始记录（HJ 828—2017）；
- 氨氮分光光度法检测记录（HJ 535—2009）；
- SS 重量法原始记录；
- 粪大肠菌群滤膜法检测记录（HJ 347.1—2018）。

共同能力：

- 文件夹/卡片式入口；
- 原始记录列表；
- 新建、编辑、删除、锁定、刷新、导出、打印；
- A4 黑白表格风格，适合打印签字和归档；
- 一手数据人工录入，耗用量、结果等按业务公式自动计算。

化验报表管理目前已有化验日报原型，日报从原始记录汇总；月报和其他统计报表尚未完成。

## 7. 重要业务规则

### 7.1 设计值、目标值、实际值

- **设计值**：水厂/工艺线建设或改扩建设计文件中的长期基准。
- **目标值**：某个工况下希望控制达到的指标值。
- **实际值**：当天采集或人工填报的一手/计算结果。
- **偏差**：实际值相对目标值的差异，不是相对设计值。
- 一组目标值的集合构成一个工况。

### 7.2 工况日期匹配

- 工况是闭区间：`起始日期 <= 业务日期 <= 结束日期`。
- 同一水厂、同一工艺线的工况区间原则上不允许重叠。
- 每日填报和诊断均应按照当天日期自动匹配。
- 日期边界、跨年度冬季工况和没有匹配的情况必须有测试。

### 7.3 指标和公式管理

- 已取消全平台中央“指标管理/指标配置”模块。
- 每个具体业务页面/看板自行管理需要展示的指标及规则。
- 统一配置弹窗至少覆盖：指标名称、单位、数据类型、是否必填、填写规范、偏差规则、是否显示，并支持增删改。
- 不提供通用的用户公式编辑器。
- 计算公式由具体业务表单固定维护，减少普通用户学习成本和错误引用数据源的风险。
- 运行数据填报只填一手数据；派生指标由系统计算。

### 7.4 偏差状态

指标可按业务选择：

- 上限管理；
- 下限管理；
- 中间目标值/正负偏差管理；
- 区间管理；
- 文本/描述性状态。

统一状态语义：

- 绿色：正常；
- 橙色：预警；
- 红色：告警。

阈值不能一刀切，应按指标配置或由业务规则固定。

### 7.5 权限初步原则

- 管理层：查看全局和各模块总体情况；
- 部门经理/专业负责人：查看并管理本专业，运行经理可配置工艺页面指标；
- 执行层：运行工、维修工、化验员等负责填报和执行，一般无配置权限。

生产运行和化验领域的细粒度权限尚未完全落地，后续必须进入后端权限模型。

### 7.6 锁定和审计

- 日报/原始记录锁定后不可被普通编辑和批量导入覆盖。
- 关键记录应保留创建人、修改人、更新时间、锁定人、锁定时间和版本。
- 删除正式业务数据应优先采用逻辑删除或受控作废，不应静默物理删除。

## 8. 当前数据持久化现状

必须明确区分：

- **安全管理**：多数核心数据已经接入 Spring Boot API 和 PostgreSQL。
- **工艺管理和化验管理**：目前仍以管理端原型和 `localStorage` 为主，尚未形成完整后端数据模型。

这意味着当前工艺/化验数据：

- 只属于当前浏览器和当前站点来源；
- 换端口、换域名、清理浏览器数据后可能看不到；
- 多用户之间不能可靠共享；
- 无法满足正式审计、并发、权限和长期归档要求。

后续工程化的第一优先级，是把工艺/化验核心数据迁移到后端数据库，而不是继续把重要状态堆到 `localStorage`。

## 9. 主要文件位置

| 用途 | 位置 |
|---|---|
| 管理端主应用和当前多数业务逻辑 | `apps/admin-web/src/App.vue` |
| 工艺诊断/生产运行样式 | `apps/admin-web/src/diagnosis.css` |
| 新模块骨架与管理质量原型样式 | `apps/admin-web/src/module-shell.css` |
| 安全管理样式 | `apps/admin-web/src/risk.css` |
| 管理端依赖 | `apps/admin-web/package.json` |
| 手机 H5 | `apps/mobile-h5/` |
| 共享 API 客户端 | `packages/api-client/` |
| Java 后端 | `backend/` |
| 数据库迁移 | `backend/src/main/resources/db/migration/` |
| 本地预览启动器 | `backend/src/test/java/com/waterx/safety/LocalPreviewLauncher.java` |
| 架构决策记录 | `docs/adr/` |
| Docker 基础设施 | `deploy/docker-compose/` |
| 根级脚本 | `package.json` |

现有 `README.md` 仍明显偏向早期“安全生产管理平台”，已经落后于当前 WaterX 综合平台定位，后续应专门重写，但本次交接不修改它。

## 10. 本地启动与验证

### 10.1 前端标准命令

项目根目录：

```bash
pnpm dev:admin
pnpm dev:h5
pnpm build:web
pnpm typecheck
```

管理端默认通常为：

```text
http://localhost:5173/
```

如果端口已被占用，Vite 可能自动改为 5174、5175 等。先检查：

```bash
lsof -nP -iTCP:5173 -sTCP:LISTEN
lsof -nP -iTCP:5174 -sTCP:LISTEN
lsof -nP -iTCP:5175 -sTCP:LISTEN
```

### 10.2 当前机器可用的 Node / pnpm

若系统终端找不到 Node/pnpm，可使用 Codex 随附运行时：

```text
/Users/fen/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node
/Users/fen/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/fallback/pnpm
```

示例：

```bash
env PATH=/Users/fen/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin:/Users/fen/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/fallback:/usr/bin:/bin \
  pnpm --filter @safety/admin-web dev -- --host 0.0.0.0
```

### 10.3 后端本地预览

标准生产式开发应使用 Java 21、Maven 和 PostgreSQL。当前机器曾使用以下现成运行时启动预览：

```text
JAVA_HOME=/Users/fen/Documents/0-AIX/安全管理/safety-platform/.tools/runtime/jdk-21.0.11+10/Contents/Home
Maven=/Users/fen/Documents/0-AIX/安全管理/safety-platform/.tools/runtime/apache-maven-3.9.16/bin/mvn
```

在 `backend/` 目录运行本地预览启动器：

```bash
env JAVA_HOME=/Users/fen/Documents/0-AIX/安全管理/safety-platform/.tools/runtime/jdk-21.0.11+10/Contents/Home \
PATH=/Users/fen/Documents/0-AIX/安全管理/safety-platform/.tools/runtime/jdk-21.0.11+10/Contents/Home/bin:/usr/bin:/bin \
/Users/fen/Documents/0-AIX/安全管理/safety-platform/.tools/runtime/apache-maven-3.9.16/bin/mvn \
test-compile org.codehaus.mojo:exec-maven-plugin:3.5.1:java \
-Dexec.mainClass=com.waterx.safety.LocalPreviewLauncher \
-Dexec.classpathScope=test
```

预览启动器会在终端输出本次随机生成的登录信息。**不要把该密码写进仓库或交接文档。**

后端健康检查通常为：

```text
http://localhost:8080/actuator/health
```

### 10.4 最低验证清单

每次完成一组本地修改后至少执行：

```bash
pnpm --filter @safety/admin-web build
```

并人工验证：

1. 登录和退出；
2. 左侧导航展开/收起及独立滚动；
3. 工况起止日期、重叠校验和日期自动匹配；
4. 运行日报新增、编辑、锁定、Excel 导入导出和分页；
5. 工艺诊断更新、状态计数和保存；
6. 工艺分析日报生成/更新；
7. 化验原始记录计算、打印布局和日报汇总；
8. 安全模块主要页面在统一 UI 后功能未回归。

## 11. 当前任务状态

最近一轮本地任务已完成的重点：

- 安全模块 UI 统一；
- 工况实施日期改为起止日期并用于自动匹配；
- 运行数据填报汇总表显示全部填报字段；
- 删除 COD 最大/最小筛选；
- 加入 Excel 批量导入/导出和分页；
- 去掉行尾重复编辑/删除；
- 工艺诊断按钮改为“更新”，增加“保存”；
- 保存后生成工艺分析日报汇总记录。
- 在所有既有模块前新增独立“生产运行”，建立班组与排班、交接班管理、当班任务、运行日志页面骨架；
- 原“生产运行”更名为“工艺管理”，现有二级菜单、页面与业务逻辑保持原样；
- 在安全管理后新增“库存管理”，建立六个规划页面骨架；
- “节能降耗”更名为“提质增效”，原规划内容保持不变；
- 经营管理补齐经营目标、生产计划、预算管理、执行分析、成本收益、回款管理入口与页面骨架；
- 新增“过程评价”，严格保留原 Excel 的运行、设备、安全、综合四个评价模块，不提前拆分检查标准；
- “管理质量”已从早期16项原型同步为 DEMO V0.1 的18项、100分结构，并拆分为独立组件、规则、样例数据和适配层；
- 新增“改进提升”，建立问题清单、改进计划、整改执行、复核关闭、改进分析页面骨架；
- 一级导航已按用户确认顺序重排，设备、化验、安全、基础信息既有入口和功能未改。

当前阶段仍只允许本地修改和预览，不提交、不推送、不部署。管理质量 DEMO V0.1 第一开发批次已完成本地构建和浏览器点击验收，详细记录见第17节。

## 12. 未完成事项与已知问题

### 12.1 高优先级

1. **工艺/化验后端化**：设计数据库表、API 和迁移脚本，逐步替代 `localStorage`。
2. **拆分 `App.vue`**：按首页、生产、化验、安全等模块拆页面和组合式函数。
3. **权限模型**：落实管理层、部门经理、执行层，以及运行经理配置权限。
4. **审计和锁定**：将日报、原始记录、诊断快照的锁定、版本和审计放入后端。
5. **README 更新**：从安全平台说明改为 WaterX 综合智慧水务平台说明。

### 12.2 工艺管理后端建议实体

- 工艺线；
- 指标定义与页面配置；
- 工艺设计标准及版本；
- 工况方案、起止日期和目标值；
- 运行日报主表和指标值明细；
- 诊断规则、诊断快照、状态统计；
- 工艺分析日报；
- 工艺调整单、执行记录和效果跟踪。

关键唯一约束建议：

- 运行日报：租户 + 水厂 + 工艺线 + 填报日期；
- 工艺分析日报：租户 + 水厂 + 工艺线 + 分析日期；
- 同一水厂/工艺线的工况日期区间不得重叠；
- 锁定记录禁止普通更新和批量覆盖。

### 12.3 化验后端建议实体

- 原始记录类型/模板；
- 原始记录主表；
- 样品明细；
- 仪器、标准物质、方法依据；
- 计算结果和公式版本；
- 锁定、复核、签字、打印版本；
- 化验日报、月报及汇总快照。

### 12.4 测试缺口

- 工况日期边界、跨年和重叠测试；
- 诊断状态计数与日报保存/upsert 测试；
- Excel 导入导出往返、错误报告、模板版本、重复数据策略；
- 历史 `localStorage` 数据兼容和迁移测试；
- COD、氨氮、SS、粪大肠菌群公式测试；
- A4 打印和 Excel 导出视觉检查；
- 各类分辨率、独立滚动和紧凑表格列宽测试；
- 安全 UI 调整后的全链路回归测试。

## 13. 下一步推荐计划

### 阶段 A：冻结和核验当前原型

1. 重新启动前后端并做一轮完整人工验收；
2. 记录当前浏览器 `localStorage` 中需要保留的原型数据；
3. 补齐关键业务规则和字段字典；
4. 将超大 `App.vue` 做第一轮无业务变化拆分；
5. 用户确认当前里程碑后，再决定是否提交本地 Git。

### 阶段 B：工艺管理后端化

1. 先落工艺线、设计标准、工况、运行日报；
2. 再落诊断规则、诊断快照和分析日报；
3. 用迁移脚本导入当前示例/原型数据；
4. 前端从 `localStorage` 切换到 API，保留短期兼容读取。

### 阶段 C：化验管理后端化

1. 优先实现四类原始记录；
2. 固定公式版本与自动计算测试；
3. 建立锁定、复核、打印归档；
4. 由原始记录生成日报，再扩展月报。

### 阶段 D：受控发布

仅在用户明确同意后执行：

```text
本地修改 → 本地测试 → 审查 Git diff → 提交/推送 GitHub → CDS 灰度部署 → 冒烟测试 → 用户验收
```

不要把每个小改动都立即发布到线上。

## 14. 禁止事项

- 未经用户明确授权，不推送 GitHub、不创建 PR、不部署 CDS。
- 不在文档、源码、日志摘要中写入账号密码、CDS Key、API Key。
- 不丢弃、覆盖或重置当前全部未提交文件的修改。
- 不用 `git reset --hard` 或破坏性清理命令处理工作区。
- 不擅自改变用户已确认的业务术语和核心规则。
- 不把派生指标重新放回一手数据填报。
- 不恢复中央统一“指标管理”菜单；指标配置留在各业务页面。
- 不用通用公式编辑器增加普通用户负担；公式按具体业务固定维护。
- 不将本地嵌入式数据库或 `localStorage` 误称为正式持久化方案。
- 不在未核对端口和进程的情况下声称本地服务已启动。

## 15. 踩坑记录

1. **项目移动过目录**
   旧路径曾是 `/Users/fen/Documents/WaterX工艺管理`，真实路径已经变为 `/Users/fen/Documents/0-AIX/WaterX工艺管理`。新会话必须以真实路径打开工作区，否则会遇到写权限、运行时和预览混乱。

2. **不要用符号链接伪装旧工作区**
   受控沙箱可能拒绝包含符号链接组件的可写根目录。正确做法是直接在真实项目目录启动 Codex 工作区。

3. **运行时路径已经变化**
   真实项目目录下目前没有原先假定的 `.tools/runtime`；现成 Java/Maven 位于旧安全项目路径。启动前应实际检查，不要照抄过时 README。

4. **本地预览密码是临时的**
   每次后端预览启动器都可能生成新密码，应从终端输出读取，不要沿用旧密码，也不要写入文档。

5. **嵌入式 PostgreSQL 不等于持久化数据库**
   后端重启后测试数据可能重建。正式验证必须准备可控的 PostgreSQL 环境和备份策略。

6. **`localStorage` 与来源绑定**
   `localhost:5173`、`localhost:5175`、CDS 域名各自拥有不同数据。端口切换后“数据不见了”不一定是业务删除。

7. **Vite 可能自动换端口**
   5173 被占用时会转到其他端口。先查看终端或 `lsof`，不要只反复打开旧地址。

8. **浏览器自动化可能受管理策略限制**
   之前曾出现内置浏览器因管理员策略无法访问本地站点。此时只能使用构建、类型检查、`curl` 和人工浏览器验证，不应绕过安全策略。

9. **表格列宽容易回归**
   工艺诊断表曾多次出现空白列过宽、偏差状态被遮挡、无必要横向滚动。修改 CSS 后必须在多个页签和左右两块看板都人工检查。

10. **业务状态不要只存在于页面展示**
    工况自动匹配、锁定、诊断统计、日报保存必须最终由后端约束，不能长期只依赖前端判断。

## 16. 新会话接手完成标准

新会话在开始写代码前，应能明确回答：

- 当前真实项目目录是什么；
- 当前分支、HEAD 和全部未提交文件是什么；
- 哪些模块有后端，哪些仍是 `localStorage` 原型；
- 当前用户要求为何是“只在本地修改和预览”；
- 工况日期和自动匹配规则是什么；
- 运行数据填报为何只保留一手数据；
- 工艺分析日报如何生成；
- 哪些操作明确禁止。

如果代码现状与本文不一致，应以**当前代码、Git 状态和用户最新指令**为准，并及时更新本文件。

## 17. 2026-08-28 DEMO V0.1 管理质量开发批次一

### 17.1 本轮产品结果

管理质量已由早期16项展示原型升级为当前讨论确认的18项、标准总分100分结构：

- 合法合规20分：`MQ-L01` 环境许可与法定义务履约率8分、`MQ-L02` 环境监管事件严重度6分、`MQ-L03` 污泥与危险废物全过程合规率6分；
- 稳定达标30分：`MQ-S01` 日均综合达标率12分、`MQ-S02` 瞬时值达标率5分、`MQ-S03` 水质稳定裕度指数8分、`MQ-S04` 处理保障与核心设备可靠性指数5分；
- 安全运行20分：`MQ-A01` 生产安全事故严重度指数8分、`MQ-A02` 重大事故隐患闭环受控率7分、`MQ-A03` 关键安全屏障有效率5分；
- 经济高效30分：`MQ-E01` 综合吨水电耗效率2分、`MQ-E02` 生化池曝气系统能效5分、`MQ-E03` 提升输送系统效率4分、`MQ-E04` 混合系统能效3分、`MQ-E05` 污泥脱水综合效率6分、`MQ-E06` 碳源利用效率4分、`MQ-E07` 除磷效率4分、`MQ-E08` 消毒资源利用效率2分。

页面继续使用既有一级菜单和顺序，没有改动工艺、化验、安全等专业模块的既有业务逻辑。根据2026-08-29最新反馈，重复度较高的“管理质量总览”已取消，管理质量只保留合法合规、稳定达标、安全运行、经济高效四个统一维度页面。

### 17.2 独立模块文件

新增目录 `apps/admin-web/src/modules/management-quality/`：

- `types.ts`：页面、维度、指标、数据状态、样例场景和改进草稿类型；
- `rules.ts`：18项指标定义、分值、公式、评分草案、适用条件、结果含义和待验证参数；
- `demo-data.ts`：三种固定、可重置的示范水厂样例数据及业务事实；
- `adapter.ts`：将规则和样例数据适配为页面视图，汇总四维得分、覆盖率、状态和主要失分项；
- `ManagementQualityPage.vue`：四维指标列表、指标详情抽屉、评分标准、结果解读、趋势、数据与业务事实下钻；
- `ImprovementDraftPanel.vue`：承接管理质量低分指标生成的问题草稿和改进计划预填内容。

`App.vue` 只保留管理质量路由接入、页面切换和改进草稿跨模块传递，没有继续堆入18项规则和样例数据。

### 17.3 三种固定演示场景

1. **场景A：稳定达标但经济性偏差**：标准总分88.5，18/18项数据可用；主要失分为污泥脱水、曝气和混合系统能效。
2. **场景B：成本较低但存在达标风险**：标准总分82.5；经济指标较好，但日均达标和水质稳定裕度下降，明确表达“低成本不等于高质量”。
3. **场景C：数据不完整与异常**：标准总分暂不发布；同时展示工艺不适用、数据不足、数据异常、计算无效、真实值为0和得分为0。

三套固定样例数据仍保留在数据层，供后续场景验证使用。根据2026-08-29最新页面简化要求，当前四维页面固定展示场景A，不再在页面顶部提供场景切换和重置控件。样例数据不代表真实生产数据或行业平均水平。

### 17.4 已完成交互

- 四个维度统一使用指标列表结构，分别展示3、4、3、8项指标；页面共享示范水厂、评价周期、`V0.1（讨论稿）`、标准总分、四维得分和数据覆盖状态；
- 统一指标详情展示定义、目的、分值、实际值、基线、偏差、单位、得分、周期、状态、来源模块、规则、结果解读和历史趋势；
- “查看评分标准”和“评分结果解读”均为真实可点击交互，展示完整公式、阈值、适用与异常处理、待验证参数及当前结果解释；
- 数据状态结构化区分正常适用、工艺不适用、数据不足、数据异常、计算无效、真实值为0、得分为0，不再共用“/”或“0”；
- `MQ-E02`、`MQ-E05` 等低分指标可下钻到工艺、设备或生产运行的演示业务事实，不在管理质量复制专业表单；
- `MQ-E02` 可发起改进并进入“改进提升 → 问题清单”，自动带入来源指标、周期、规则版本、当前值、当前得分、适用基线和问题描述；点击“转为改进计划”后继续带入建议目标。

本版没有设置星级、独立数据质量得分、全局红线，也没有把过程评价并入管理质量100分。

### 17.5 构建与浏览器验收

2026-08-28 本地验证结果：

- `pnpm --filter @safety/admin-web build`：通过；`vue-tsc --noEmit` 和 Vite 生产构建均成功，81个模块完成转换；
- `git diff --check`：通过，无空白符错误或冲突标记；
- 本地健康检查：管理端 `http://localhost:5173/` 返回200，后端 `http://localhost:8080/actuator/health` 返回200；
- 实际登录：通过；
- 一级菜单与管理质量四个维度入口：通过；
- 四维页面：分别验证3、4、3、8行，共18项；
- 指标详情、评分标准、结果解读、历史趋势：通过；
- 三套固定场景数据与默认场景A：通过；页面已按最新要求取消顶部场景切换和重置控件；
- 场景C七类状态汇总及“标准总分暂不发布”：通过；
- `MQ-E02` 业务事实下钻：通过，能查看工艺运行明细和设备性能核查两条记录；
- 发起改进：通过，已实际进入问题清单并继续进入改进计划，预填字段均可见；
- 浏览器控制台：未发现警告或错误。

本轮验证使用桌面尺寸检查管理端布局，视觉延续 WaterX 蓝色、浅灰、白色的紧凑专业风格。当前复用已运行的5173前端和8080后端进程，没有无故中断现有预览。

### 17.6 当前已知限制

- 18项规则、阈值、插值和部分适用条件仍是 `V0.1（讨论稿）`，页面均标识“待验证参数”，不能作为正式企业标准；
- 当前三场景使用前端固定样例数据，尚未接入正式管理质量API、规则版本库或结果发布流程；
- 专业业务事实为明确标识的演示明细，后续应通过对象标识下钻至工艺、化验、设备、安全、库存等真实记录；
- 发起改进当前形成可见的跨模块草稿与计划预填，尚未持久化到后端，也未建设完整执行、复核和效果回看流程；
- 场景C存在应有数据不足或异常时，总分暂不发布；工艺不适用项的正式折算仍需专家评审和多厂数据验证；
- 临时预览账号由本地启动器生成，密码不写入本文；后端重启后应从新启动终端读取最新凭据。

## 18. 2026-08-29 过程评价 V0.1 主产品开发

### 18.1 本轮产品结果

过程评价已从4个规划骨架升级为主产品内可运行的完整业务链路，并按最新专项设计调整为5个独立模块：

- 运行管理评价：7类、25项；
- 设备管理评价：7类、25项；
- 化验管理评价：7类、29项；
- 安全管理评价：7类、27项；
- 综合管理评价：7类、25项；
- 合计131项检查要求。

五个模块分别按100分计算，当前不计算五模块简单平均总分。分类权重、检查项重要程度系数（关键5、重要3、一般1）、结果状态和模块状态规则均按专项基线实现。关键控制失效不能被一般项高分抵消。

过程评价二级入口现为：运行管理评价、设备管理评价、化验管理评价、安全管理评价、综合管理评价、问题与整改、评价报告。独立“评价总览”入口已于2026-08-29取消。

### 18.2 独立模块文件

新增目录 `apps/admin-web/src/modules/process-evaluation/`：

- `types.ts`：页面、模块、任务、检查结果、问题整改、复核结论和报告摘要类型；
- `rules.ts`：五模块、35个分类、131项规则、权重、重要程度、边界和页面映射；
- `demo-data.ts`：固定且可重置的综合评价任务、131项检查结果和跨模块示范问题；
- `adapter.ts`：本地状态读写、计分、任务创建、报告发布门槛、唯一主责问题、整改提交和复核关闭；
- `ProcessEvaluationPage.vue`：过程评价模块总入口和主产品导航适配；
- `ProcessScoreDock.vue`：五模块独立得分、状态、完成数量、吸顶展示和模块切换；
- `ProcessTaskWorkspace.vue`：模块/分类页签、规则列表、结构化事实、结果判定和问题生成；
- `ProcessIssueCenter.vue`：统一问题台账、整改提交、复核退回/关闭/升级；
- `ProcessReport.vue`：五模块独立画像、管理判断、优先整改和发布阻塞项；
- `TaskCreateModal.vue`：综合检查、专项检查和整改复核任务创建；
- `process-evaluation.css`：WaterX蓝、浅灰、白的紧凑专业视觉及响应式布局。

`App.vue` 只新增过程评价页面类型、组件接入、二级导航和专业事实下钻映射，没有把131项规则或流程状态继续堆进主文件。既有管理质量、工艺、化验和安全逻辑保持原状。

### 18.3 已完成业务链路

- 五模块独立得分和状态集中到顶部评分导航，并可直接切换专业评价页面；
- 新建任务的数据结构、规则冻结和创建能力继续保留，可选择综合检查、专项检查或整改复核；当前可见“新建评价任务”按钮按最新要求暂时移除，后续重新确定入口；
- 五模块均提供7个分类页签，页面逐项呈现全部131项规则；
- 检查结果明确区分待检查、证据不足、符合、部分符合、不符合和不适用；不适用必须填写客观原因；
- 支持抽查对象数、符合数量、系统建议结果、检查对象、事实、证据、问题标签和暂存/保存下一项；
- 部分符合或不符合可形成唯一主责问题，并可选择其他模块作为关联风险；关联模块不重复扣分；
- 统一问题台账支持待整改、整改中、待复核、已关闭和升级处理，整改人与复核人不能相同；
- 复核支持关闭、退回整改和升级处理，保留整改反馈与复核意见；
- 报告按五模块分别展示过程画像，不计算总平均分；存在待检查、证据不足、不适用原因未确认、重大问题未落实控制或待复核事项时禁止提交审批；
- 运行检查项已实际下钻到现有“工艺管理 → 运行数据填报”，复用原始业务事实，不复制专业表单。

跨模块边界已固化到规则和界面提示：第三方在线水质监测运维监督全部归化验管理；一般过程仪表归设备管理；达标率、电耗、利润、回款等结果指标不进入过程评价；同一事实只保留一个主责问题和一次主扣分。

### 18.4 固定示范数据

默认任务为“2026年8月综合过程评价”，覆盖五模块131项规则，包含：

- 运行重要调控审批缺失；
- 设备重复故障根因分析不足，并关联运行风险；
- 化验平行样记录缺少复核签字（已关闭）；
- 安全有限空间救援装备超期，并关联设备风险；
- 待检查、证据不足、不适用、部分符合和不符合等典型状态。

固定示范数据和重置能力仍保留在适配层。当前任务、检查结果和问题使用前端 `localStorage` 持久化，不代表真实生产数据；当前页面不再展示总览任务列表或重置按钮。

### 18.5 构建与浏览器验收

2026-08-29 本地验证结果：

- `pnpm --filter @safety/admin-web build`：通过；`vue-tsc --noEmit` 和Vite生产构建均成功，97个模块完成转换；
- `git diff --check`：通过；新增过程评价文件另行完成空白符检查；
- 规则数量：运行25、设备25、化验29、安全27、综合25，共131项；分类页签逐一点击核对为4+3+6+4+4+2+2、4+4+5+4+3+3+2、4+4+5+6+4+3+3、4+3+4+5+3+2+6、4+4+4+4+4+3+2；
- 本地后端 `http://localhost:8080/actuator/health` 与管理端 `http://localhost:5173/` 已重新启动；
- 实际登录：通过；
- 五模块顶部评分导航、五模块页面、35个分类页签、规则详情和六类检查结果：通过；
- 新建评价任务及规则版本冻结能力：历史验证通过；当前可见按钮按最新要求暂时移除；
- 结构化数量建议、保存检查结果和主责问题生成：通过；
- 关联风险不重复扣分：通过；
- 整改提交、独立复核人、复核关闭：通过；
- 报告五模块独立画像及发布门槛：通过；
- 运行专业事实下钻：通过；
- 浏览器控制台：未发现警告或错误。

临时预览账号由本地启动器生成，本节不记录密码。后端重启后必须从启动终端读取最新凭据。

### 18.6 当前已知限制与后续校准

- 131项规则和分类权重为 `V0.1` 首轮试运行基线，抽样数量、状态阈值和关键控制映射仍需多厂试跑与专业审核后再冻结新版本；
- 当前规则页保存结构化事实和证据引用文本，不上传附件，也未接入自动取数、外部数据接口或复杂评分自动化；
- 过程评价任务、检查结果、问题和报告当前保存在前端 `localStorage`，尚未建设后端表结构、正式权限、审计日志和跨用户协作；
- 设备和综合专业模块的原始业务详情入口仍在规划中，当前使用明确标识的评价演示事实承接；运行、化验、安全已具备可复用的现有专业页面；
- 历史示范任务只用于任务列表和报告入口展示，尚未配置完整历史131项快照；
- 报告目前为页面预览和提交门槛演示，尚未生成正式PDF、电子签署或审批发布记录；
- 正式上线前仍需基于真实检查样本复核“部分符合”比例、问题等级、关闭条件和跨模块主责判断。

### 18.7 评价总览标题区视觉调整

2026-08-29 根据本地预览反馈，对过程评价总览顶部标题区完成局部重设，不涉及业务逻辑：

- `ProcessOverview.vue` 为标题内容增加独立结构标识；
- `process-evaluation.css` 将标题区调整为与管理质量等模块一致的白色/浅蓝渐变卡片，统一边框、圆角、阴影和内容留白；
- 标题、说明文字与右侧操作区改为垂直居中对齐，操作区增加轻量分隔，并统一“重置示范数据”“新建评价任务”的尺寸；
- 修正通用按钮颜色优先级，确保主按钮稳定显示为白字蓝底；
- 补充窄屏布局，标题与按钮在小屏下自动上下排列，两个按钮等宽显示。

本次调整后重新完成管理端类型检查和Vite生产构建，97个模块转换成功；`git diff --check`及新增文件空白符检查通过。实际打开本地评价总览核验，标题内容区与操作区中心对齐，按钮样式正确，浏览器控制台未发现警告或错误。

### 18.8 过程评价入口集中与检查项文字优化

2026-08-29 根据第二轮本地预览反馈完成以下结构和视觉调整，不改变131项规则、计分、问题生成、整改复核或报告逻辑：

- 运行、设备、化验、安全、综合五个专业评价页面删除“执行检查 / 问题清单 / 评价报告”重复页签行，进入专业模块后直接呈现评分边界、分类和检查工作区；
- 左侧过程评价菜单将“整改中心”更名为“问题与整改”，并在其下新增独立“评价报告”入口；
- 新增 `evaluationReport` 页面标识，统一入口、页面内部跳转和历史任务报告打开时均可正确同步左侧导航选中状态；
- 问题页面主标题统一为“问题与整改”，总览和报告中的相关按钮文案同步调整；
- A01、B01等检查项编号使用明确的标准蓝，检查项名称改为深蓝灰、13px和700字重，状态文字继续使用独立绿/黄/红语义色，避免正文与状态混色或显示过浅；
- 清理已不再使用的专业模块页签样式和事件接口，避免残留重复结构。

本次调整后管理端类型检查及Vite生产构建通过，97个模块完成转换。实际点击核验运行管理评价、设备管理评价、问题与整改、评价报告及问题/报告双向跳转，导航选中状态正确；专业页面内重复页签数量为0，浏览器控制台未发现警告或错误。

### 18.9 取消管理质量总览

2026-08-29 根据本地预览反馈，管理质量中的“管理质量总览”与四个维度页面重复度较高，已整体取消：

- 左侧管理质量二级菜单删除“管理质量总览”，仅保留合法合规、稳定达标、安全运行、经济高效；
- 删除 `qualityOverview` 页面类型和页面规则定义，防止旧入口继续被识别为有效管理质量页面；
- 删除总览专属的主要失分项、18项指标状态和趋势摘要展示区；
- 管理质量页面内部维度切换由五项调整为四项，并保持桌面和窄屏布局适配；
- 四个维度页面继续共享标准总分、四维得分和规则讨论稿状态，18项指标详情、评分标准、结果解读、业务事实及发起改进能力均保持不变。

本次调整后管理端类型检查和Vite生产构建通过，97个模块完成转换。实际逐页核验合法合规3项、稳定达标4项、安全运行3项、经济高效8项，共18项；四页均无总览摘要区，指标详情与评分标准可正常打开，浏览器控制台未发现警告或错误。

### 18.10 管理质量四维页面进一步简化与评分导航吸顶

2026-08-29 根据本地预览反馈，将合法合规、稳定达标、安全运行、经济高效四个页面统一收敛为“固定评分导航 + 当前维度指标表”两层结构：

- 删除四维页面顶部的大标题说明框及规则标签区；
- 删除示范水厂字段，当前项目继续由平台全局顶部栏统一表达；
- 删除演示场景、重置样例和场景说明区域，当前页面固定展示默认场景A；三套固定场景数据仍保留在数据层；
- 删除页面内部重复的合法合规、稳定达标、安全运行、经济高效横向切换条；
- 删除数据覆盖状态卡片；
- 页面顶部重新组合为单行6块：评价周期、标准总分、合法合规得分、稳定达标得分、安全运行得分、经济高效得分；
- 四个维度得分卡继续作为当前指标内容的切换入口，选中状态通过边框、底色和进度条明确表达；
- 整行评分导航设置为内容区吸顶，纵向滚动指标表时固定在平台全局顶部栏下方；窄屏时保持单行并支持横向滚动。

本次调整后管理端类型检查和Vite生产构建通过，97个模块完成转换。实际核验顶部大标题、旧上下文区、重复维度切换条和数据覆盖卡均已移除；6个顶部块处于同一行，四维卡片切换分别正确显示3、4、3、8项指标。经济高效长页面滚动后，内容区滚动位置约180px时评分导航仍固定在全局顶部栏下方，浏览器控制台未发现警告或错误。

### 18.11 一级导航顺序调整

2026-08-29 将“经营管理”移动到“库存管理”下方、“提质增效”上方。当前一级菜单顺序为：

首页 → 生产运行 → 工艺管理 → 设备管理 → 化验管理 → 安全管理 → 库存管理 → 经营管理 → 提质增效 → 过程评价 → 管理质量 → 改进提升 → 基础信息。

本次只调整一级导航显示位置，经营管理和提质增效的二级菜单、页面和业务逻辑均未改变。管理端类型检查和Vite生产构建通过，实际刷新本地页面核验菜单顺序正确，浏览器控制台未发现警告或错误。

### 18.12 取消过程评价总览并改为五模块评分导航

2026-08-29 根据本地预览反馈，过程评价取消独立“评价总览”，参照管理质量的紧凑切换方式重组页面：

- 左侧过程评价二级菜单删除“评价总览”，仅保留五个专业评价、问题与整改和评价报告；
- 删除 `evaluationOverview` 页面类型、路由识别和 `ProcessOverview.vue`；
- 删除原总览中的“过程评价总览”标题区、当前任务、总体进度、待整改问题、待复核、评价工作流和评价任务列表；
- 新增独立 `ProcessScoreDock.vue`，集中展示运行管理94.0、设备管理99.0、化验管理94.2、安全管理95.5、综合管理95.7等当前五模块得分、状态和完成数量；
- 五个评分磁贴可直接切换相应专业评价页面，并同步左侧导航选中状态；
- 评分磁贴整行设置为内容区吸顶，在专业检查、问题与整改及评价报告页面上方统一显示；进入问题或报告时不默认选中某个专业模块，点击磁贴即可回到该专业评价；
- 专业模块页删除重复的“返回总览”和当前预估分卡片；问题与整改、评价报告删除“返回总览”按钮；
- “新建评价任务”按钮当前从界面删除，但 `TaskCreateModal`、任务创建适配逻辑、任务类型与规则版本冻结能力继续保留，等待后续确定新入口。

本次调整后管理端类型检查和Vite生产构建通过，97个模块完成转换。实际核验左侧无“评价总览”，原总览标题、摘要、流程、任务列表和新建按钮均不存在；五个磁贴同排显示，运行与设备磁贴切换及侧栏同步正常。设备评价长页面滚动620px后，评分导航固定在全局顶部栏下方；问题与整改、评价报告均可正常打开并通过磁贴返回专业模块，浏览器控制台未发现警告或错误。

### 18.13 专业评价页面标题与评分边界精简

2026-08-29 根据本地预览反馈，进一步删除运行、设备、化验、安全、综合五个专业评价页面中与顶部评分磁贴重复的信息：

- 删除各专业页面的任务状态、任务名称、规则版本、模块名称和模块说明标题卡；
- 删除各专业页面的“评分边界”提示条；
- 清理不再使用的模块标题卡、预估分卡和评分边界样式；
- 顶部五模块评分磁贴之后直接进入7个分类页签和检查工作区；
- 131项规则、分类权重、检查要求、结构化判定、业务事实、问题生成和专业下钻逻辑保持不变。

本次调整后管理端类型检查和Vite生产构建通过，97个模块完成转换。实际逐一打开运行、设备、化验、安全、综合五个页面，标题卡和评分边界数量均为0，五模块评分磁贴和每个模块7个分类页签正常显示，浏览器控制台未发现警告或错误。

### 18.14 评价结果管理与多期评价任务

2026-08-29 根据过程评价实际会按月度、季度、半年度及专项多次开展的业务需求，新增“评价结果管理”，并将其置于过程评价二级菜单首位。过程评价当前入口顺序为：评价结果管理、运行管理评价、设备管理评价、化验管理评价、安全管理评价、综合管理评价、问题与整改、评价报告。

本轮采用“一个评价任务可选择多个模块，结果列表按模块拆成多行”的结构：任务负责冻结水厂、评价日期、评价周期、评价频次、评价范围和规则版本；每一行模块评价独立展示分数、完成数量和状态。主要能力包括：

- 评价结果列表按评价日期倒序汇总历次任务，展示任务名称/编号、评价模块、任务性质、评价周期、得分、状态、负责人和规则版本；
- 支持按任务名称/编号/负责人搜索，并按评价模块、评价频次和任务状态筛选；
- 新建评价任务支持月度、季度、半年度、专项和整改复核五类频次，以及综合检查、专项检查和整改复核三类任务性质；
- 支持任务信息编辑；任务一旦开始，评价范围保持冻结，只允许修改名称、日期、频次和负责人；
- 草稿或待执行且没有录入事实的问题可直接删除；已录入检查结果或已产生问题的任务执行“作废”并保留历史记录；
- 无发布阻塞项的任务可锁定，锁定后检查项、问题整改和评价报告统一只读，不能再编辑、删除或作废；
- 点击任一历史结果行可在现有131项专业评价工作区中打开当期快照，不另建一套历史详情页；
- 顶部五个评分磁贴分别显示各模块最新一次未作废评价，可直接切换到对应当期内容；侧栏进入专业模块时默认选择该模块最新评价；
- 本地旧版 `localStorage` 数据会自动补齐评价日期、频次、更新时间和锁定/作废字段，并为已完成但缺少明细的历史示范任务补齐只读评价快照，不覆盖已有检查记录或问题；
- 固定示范数据新增月度综合评价、季度综合评价、专项评价和整改复核等多期组合，便于展示同一模块一年内多次评价。

新增 `EvaluationResultsManager.vue`；`types.ts` 增加评价频次、锁定/作废状态和模块结果摘要；`adapter.ts` 增加旧数据迁移、最新评价选择、结果扁平汇总、任务编辑、删除/作废和锁定逻辑；`TaskCreateModal.vue` 升级为新建/编辑共用表单；`ProcessScoreDock.vue` 改为按模块最新评价展示；`ProcessTaskWorkspace.vue`、`ProcessIssueCenter.vue` 和 `ProcessReport.vue` 增加锁定任务只读保护。

本节所记录的“暂不修改131项文案”限制已由后续18.15解除；用户已经正式确认“检查要求可执行化 V0.2”，主产品现以确认后的V0.2细化规范和五份规则明细为新基线。

验证结果：管理端 `vue-tsc --noEmit` 与Vite生产构建通过，99个模块完成转换；`git diff --check`通过，过程评价新增文件未发现行尾空白。实际浏览器核验评价结果列表、模块筛选、新建单模块评价、任务编辑、未开展任务删除、已完成任务锁定、锁定后检查/问题/报告只读、历史行回看、顶部磁贴返回最新评价和未完成任务锁定阻塞均正常；页面未出现运行时错误提示。现有本地前端预览仍使用 `http://localhost:5173/`，后端仍使用 `http://localhost:8080/`。

### 18.15 过程评价检查要求可执行化 V0.2

2026-08-29 用户正式确认《WaterX过程评价_检查要求细化规范_V0.2.md》和《WaterX过程评价_检查要求V0.2预览.html》，此前18.14中“暂不固化131项文案”的限制已经解除。本轮以五份V0.1规则明细作为业务事实来源，将检查要求表达和录入交互升级为 `PX-V0.2（检查要求可执行化）`，未改变五模块数量、分类权重、计分框架、任务管理和问题闭环逻辑。

规则与数据实现：

- `types.ts` 新增规则详情、客观事实字段类型和判定依据模型；`CheckRecord` 新增 `factValues`，旧本地数据由适配层自动补齐空对象；
- 新增 `rule-details.ts` 统一聚合入口，以及 `rule-details-operations.ts`、`rule-details-equipment.ts`、`rule-details-laboratory.ts`、`rule-details-safety.ts`、`rule-details-general.ts` 五份模块规则详情；
- 五模块详情数量固定为运行25、设备25、化验29、安全27、综合25，共131项；每项均集中维护要求概述、3—6个具体核查要点、检查范围与抽样、客观事实字段、符合/部分符合/不符合/不适用判定、关键控制、问题标签、最低证据、关闭条件、判定方式和模块边界；
- `rules.ts` 将规则版本更新为 `PX-V0.2（检查要求可执行化）`，创建新评价任务时冻结该版本；规则文本与页面组件分离，后续校准不需要重写页面。

检查工作台实现：

- 左侧检查列表仅显示编码、名称、当前状态和问题数，不再把长段检查要求挤入列表；
- 右侧详情按“要求概述 → 具体核查要点 → 检查范围与抽样 → 客观事实记录 → 检查结果判定 → 异常问题 → 客观判定依据与关闭条件”统一呈现；3—6个核查要点默认展开可见；
- 客观事实根据每项规则动态显示数量、天数、日期、文本和是/否字段，检查人员不直接输入任意百分制分数；存在明确分子/分母关系时，系统给出符合、部分符合或不符合建议，最终仍由检查人员确认；
- 符合项只保留可简写的最低证据，不要求长说明；证据不足和不适用分别填写待补证据或客观原因；
- 部分符合或不符合时显示结构化异常区，记录问题对象、异常事实、证据、建议标签、问题等级、责任人、期限和关联风险模块；继续执行“一项事实只生成一个主责问题和一次主扣分”；
- 客观判定依据明确展示四类结果边界，适用时另行显示关键控制条件；关闭条件和专业模块边界同步可见；
- 既有专业事实下钻、评价结果管理、多期任务、锁定只读、问题整改、复核关闭和评价报告链路均保留。

本轮验证结果：

- 管理端 `vue-tsc --noEmit` 与Vite生产构建通过，105个模块完成转换；仅保留既有的大包体积提示，没有类型、模板或构建错误；
- `git diff --check`通过；五份规则详情静态完整性检查均通过，131项全部具备3—6个核查要点、至少1个客观事实字段、四类判定依据、最低证据和关闭条件；
- 浏览器实际逐一打开运行、设备、化验、安全、综合五个评价页面，均显示V0.2标识、要求概述、默认展开核查要点、抽样、动态事实字段和客观判定依据；
- 运行 `OPS-C05` 实际核验为“部分符合”，左侧显示1个问题，右侧正确回显对象、异常事实、证据、标签、责任人、期限、关联风险及唯一主责问题；进入“问题与整改”后可看到同一事项并继续整改/复核；
- “评价结果管理”与新建任务入口正常，新建弹窗显示 `PX-V0.2（检查要求可执行化）` 和五模块项数；
- 浏览器开发日志仅有Vite连接和热更新调试信息，未发现 warning 或 error。

当前限制与后续校准：

- 当前仍不接自动取数、外部接口、附件上传和复杂评分自动化；动态事实字段与系统建议用于本地可运行原型，正式上线前需用真实检查样本逐项校准字段名称、单位、分子分母识别和关键控制映射；
- 本地任务、记录和问题仍保存在前端 `localStorage`；正式后端需要保存任务规则快照。当前旧版历史任务可保留原规则版本标识，但详情读取的是当前前端规则基线，不能替代生产级不可变审计快照；
- 规则来源文件标题仍为V0.1明细，V0.2指本轮已经确认的“可执行化表达与交互结构”；业务事实没有脱离五份明细自行扩展；
- 本地前端预览继续使用 `http://localhost:5173/`，后端继续使用 `http://localhost:8080/`。临时账号密码只从当前启动终端读取，不写入本文件；本轮未提交、未推送、未部署。

### 18.16 当前本地预览运行状态

2026-08-29 15:36（Asia/Shanghai）已重新启动本地后端预览并完成登录验证，当前交付状态如下：

- 管理端前端：`http://localhost:5173/`，访问返回HTTP 200；前端沿用原有Vite进程，本次未中断或重启；
- 本地后端：`http://localhost:8080/`，使用 `LocalPreviewLauncher`、临时嵌入式PostgreSQL和18项数据库迁移启动；
- 临时用户名固定为 `preview_admin`；临时密码由每次后端启动随机生成，本文件不记录密码，必须从当前启动终端读取；
- 使用当前新生成的账号调用 `/api/v1/auth/login` 实际验证返回HTTP 200，确认用户名、密码和后端服务有效；
- 当前预览只适合本地确认，后端进程结束、终端关闭、电脑休眠或重启、端口被占用时可能需要再次启动；每次重启后密码都会变化；
- `scripts/start-preview.sh` 当前仍指向项目内不存在的 `.tools/runtime`。本次使用机器上已有的同版本JDK 21.0.11和Maven 3.9.16直接启动；后续若要一键恢复预览，应先修正启动脚本的运行时发现逻辑，不能假设项目内一定存在 `.tools/runtime`；
- 本轮保存的是现有全部未提交本地开发成果和验证状态；没有创建Git提交、没有推送GitHub、没有部署CDS。

### 18.17 GitHub与CDS发布授权

2026-08-29 用户已明确要求将截止当前的全部成果提交到GitHub，并发布到CDS一次。本节授权仅覆盖当前已核验的WaterX工作树，覆盖本文前部“仅本地修改、不提交、不推送、不部署”的阶段性限制。

- GitHub远端：`https://github.com/stone6484/WaterX.git`；
- 发布分支：`codex/release-waterx-20260816`；
- 发布内容：当前工作树中的管理质量、过程评价、导航与UI调整、规则数据、适配层、依赖锁文件及本HANDOFF；
- 发布前基线：管理端类型检查与Vite生产构建通过，105个模块完成转换；`git diff --check`通过；未发现临时预览密码、CDS Project Key或API Key进入仓库；
- 发布动作：提交并推送上述分支后，使用官方 `cdscli` 对WaterX现有CDS项目执行全量部署、就绪检查和冒烟测试；实际Git提交哈希、CDS分支状态和预览地址以Git与CDS操作记录为准。

### 18.18 化验管理—原始记录管理 V0.1（已验收发布）

2026-08-30 在真实主产品中完成“化验管理—原始记录管理”第一版可运行实现。该功能最初按本地开发和预览范围实施；用户完成本地检查后，已明确授权将截止当前的全部成果提交并推送至GitHub，同时发布到CDS。功能范围仍不包含化验报表、日报、化验任务、水质分析或基础配置。

模块与文件：

- `apps/admin-web/src/modules/lab-raw-records/types.ts`：18类模板、方法状态、记录状态、质控、审计留痕的数据模型；
- `apps/admin-web/src/modules/lab-raw-records/templates.ts`：Y01—Y18方法、字段、公式版本、结果项、固定示范值和适用性说明的集中配置；
- `apps/admin-web/src/modules/lab-raw-records/formula-engine.ts`：受控计算与质控检查，页面不直接写公式；
- `apps/admin-web/src/modules/lab-raw-records/LabRawRecordsPage.vue`：模板库、台账、A4记录表、复核锁定、更正版本和审计留痕；
- `apps/admin-web/src/modules/lab-raw-records/lab-raw-records.css`：沿用WaterX蓝/浅灰/白视觉及A4横版黑白打印规则；
- `apps/admin-web/src/App.vue`：仅增加新模块入口，旧四类原始记录实现保留为不可达兼容分支，既有化验报表逻辑未改动。

18类记录范围：

- 既有4类统一优化：Y03 COD、Y04 SS、Y06氨氮、Y09粪大肠菌群；可从旧 `waterx-lab-original-records` 数据迁移到统一模型，检测人示范名称由“运行工”纠正为“化验员”，新增方法/公式版本、质控、复核人与检测人分离、不可直接解锁、锁定后更正派生新版本和审计事件；既有计算业务口径保持不变；
- 本轮新增14类：Y01 pH、Y02 BOD₅、Y05总磷、Y07硝酸盐氮、Y08总氮、Y10活性污泥指标、Y11泥饼指标、Y12正磷酸盐、Y13碱度、Y14氯化物、Y15总盐量、Y16 PAC、Y17次氯酸钠、Y18碳源；
- 分类固定为水质13类、污泥2类、药剂3类，编号与台账资料 `CQSWHY2026-Y01` 至 `CQSWHY2026-Y18` 对齐；固定示范数据保存在浏览器 `waterx-lab-raw-records-v04`，每个项目可单独重置。

计算、质控和受控流程：

- 所有黄色输入区仅填写现场示值、称量、滴定、培养或仪器读数等一手观测值；蓝灰结果区由 `formula-engine.ts` 自动生成，不能人工改写；
- 已实现pH平行平均、BOD₅、COD、SS、分光曲线、220/275 nm校正、菌群换算、MLSS/MLVSS/SVI、泥饼含水率/有机物、碱度、氯化物、总盐量、PAC氧化铝、次氯酸钠有效氯和冰醋酸含量等受控计算；
- PAC、次氯酸钠和冰醋酸公式已按提供的药剂原始记录与设计原型复核：PAC示范氧化铝12.88%，次氯酸钠有效氯12.23%，冰醋酸含量99.88%；PAC盐基度因未取得对应原始滴定字段，本轮明确预留，不使用暂定公式；
- 质控统一区分待完成、通过、警告和阻断，检查必填完整性、公式有效性及项目特定条件；pH平行差、BOD₅剩余DO、SS恒重差、菌落可计数范围等限值显示为演示控制参数，待真实样本校准；
- 工作流为草稿→待复核→已复核→已锁定；复核人不得与检测人相同；锁定记录不能直接编辑或解锁，只能填写原因后派生V2及后续更正版本，原锁定版本和审计事件保留；
- 硝酸盐氮、碱度、总盐量/总固体/TDS边界、碳源产品类型继续显示“待确认/项目方法”。碳源选择乙酸钠或复合碳源时不会套用冰醋酸公式，结果转为待计算并阻断提交。

A4与打印：

- 原始记录表按A4横版宽度设计，屏幕显示人工输入、自动结果、方法/公式版本、质控、签字和更正原因；
- 打印规则为 `A4 landscape`、8 mm页边距，强制文字/边框黑色、输入和结果背景白色、隐藏工具栏和日期控件图标，表格表头可重复，关键表格/质控/签字区禁止跨页拆分；内容较多的记录允许自动续到下一张A4横版，不压缩到不可读字号；
- 浏览器实际点击“打印/预览”无运行时错误，并从页面样式表核验上述 `@page` 与黑白打印规则已生效。

本轮验证：

- 管理端 `vue-tsc --noEmit`通过；Vite生产构建通过，110个模块完成转换；仅有既有的大包体积提示；
- `git diff --check`通过；
- 使用本地后端随机临时账号实际登录，冷重载后模板卡片为18张，分类计数为13/2/3；逐项核验pH、活性污泥、PAC、次氯酸钠、碳源的输入、自动结果、质控与适用性说明；
- 实际跑通pH记录提交复核、复核人分离、锁定归档、发起更正V2和审计留痕；全新浏览器会话检查控制台无 warning 或 error；
- 当前管理端 `http://localhost:5173/`、后端 `http://localhost:8080/` 已启动。临时账号密码仅从当前启动终端读取，不写入HANDOFF。

发布目标：

- GitHub远端：`https://github.com/stone6484/WaterX.git`；
- 发布分支：`codex/release-waterx-20260816`；
- CDS：沿用WaterX既有项目和分支配置，通过官方 `cdscli` 执行全量部署、就绪检查与冒烟测试；提交哈希、CDS运行状态和预览地址以本次Git及CDS操作记录为准。

待用户决策/后续校准：

- 硝酸盐氮最终采用离子色谱还是项目紫外方法；
- 碱度的受控方法文件及CaCO₃/CaO结果口径；
- Y15最终保留“总盐量”，还是拆分总固体、总盐量和溶解性总固体三张记录；
- PAC盐基度原始滴定字段与公式、各项目真实质控限值；
- 实际采购的碳源产品类型、对应产品标准、COD关联方式和合同判定指标；
- 正式后端存储、不可变版本快照、电子签名及档案保存期限仍未开发，当前记录保存在本地浏览器。

### 18.19 提质增效—高效沉淀池分析 V1.0（本地首版）

2026-08-31 已按 `/Users/fen/Documents/WaterX工艺管理/docs/提质增效/高效沉淀池设计包_V1.0/` 的产品需求、技术设计、API契约、机器模型、规则执行契约、追溯矩阵和三个配套工作簿，将“高效沉淀池分析”接入WaterX主产品“提质增效”二级导航。本轮只完成本地开发和预览，没有创建Git提交、没有推送GitHub、没有部署CDS；过程评价、管理质量、化验管理及其他既有业务逻辑均未改动。

模块文件与接入：

- `apps/admin-web/src/modules/high-efficiency-sedimentation/HighEfficiencySedimentationPage.vue`：分析概览、数据与计算、偏差诊断、优化方案、效果跟踪五页签及完整演示交互；
- `types.ts`：字段、指标、五类评价状态、规则结果、原因核验、优化动作和效果验证类型；
- `hsc-model-0.1.0.json`、`model.ts`：直接纳入已确认的机器模型并提供只读适配，固定为60字段、29指标、15基准、20规则、26原因；模型状态保持 `DRAFT`，未把待确认经验值包装为正式标准；
- `demo-data.ts`：三套固定且可重置的示范场景，分别为“稳定出水但药耗偏高”“药剂证据不足”“进出水数据不可比”；
- `engine.ts`：集中计算29项指标并执行20条规则状态映射，页面不直接维护公式与规则；
- `adapter.ts`：使用 `waterx-hsc-v1-demo-state` 保存本地原因核验、方案和效果验证状态，为后续API接入保留单一适配层；
- `high-efficiency-sedimentation.css`：延续WaterX蓝/浅灰/白专业视觉，隔离主产品全局 `article`、`aside`、`header` 样式，支持桌面和900像素窄屏；
- `apps/admin-web/src/App.vue`：仅新增“提质增效—高效沉淀池分析”导航和页面挂载；`apps/admin-web/tsconfig.json` 仅增加JSON模块读取能力。

实现范围与规则边界：

- 分析概览统一展示对象、周期、场景、数据完整度、字段/指标覆盖、核心运行画像、优先诊断、优化机会和单体诊断边界；
- 数据与计算按对象、配置、设计、构筑物、设备、药剂、成本、运行、水质、控制、污泥、能耗和现场观察分组展示字段来源、覆盖率、核验状态及29项公式结果；理论药量、摩尔比、理论产泥和平衡偏差因化学计量/产泥参数未确认，明确显示“缺少输入/待项目确认”，不发布伪精确值；
- 偏差诊断完整加载20条规则，统一区分 `NOT_APPLICABLE`、`DATA_INSUFFICIENT`、`NORMAL`、`TRIGGERED`、`SUPPRESSED`，展示证据链、原因核验、建议、约束、恢复条件和专业边界；
- `HSC-DATA-01` 命中时只提示补齐有效成分、投加计量和检测结果，返回 `DATA_INSUFFICIENT` 并抑制 `HSC-CHEM-01—04`；`HSC-DATA-02` 命中时只提示修正采样点、时段和HRT配对，返回 `DATA_INSUFFICIENT` 并抑制 `HSC-HRT-01/02`、`HSC-SET-01/02`；两条数据规则均不能发起业务优化方案；
- 原因核验支持待核验、已确认、已排除、证据不足和不适用五种状态；业务规则可带入来源、证据、目标、步骤、守护指标、停止条件和回退条件生成优化方案；
- 优化方案支持草稿、待审批、执行中、待验证、已完成和已回退状态；效果跟踪展示四项可比性检查、优化前后指标和验证年化节约，标记不可比后会清空验证收益并禁止固化目标，重新满足可比性后才可继续接受；
- 模块只形成高效沉淀池单体水力、混合絮凝、沉淀、药剂效率、回流排泥、能耗成本与优化验证结论；全厂水质、生化工艺、全厂TP原因、设备维修质量、化验管理、采购和安全评价只引用，不重复形成主诊断。

本轮验证结果：

- `pnpm --filter @safety/admin-web typecheck`通过；`pnpm --filter @safety/admin-web build`通过，118个模块完成转换；仅有既有的大包体积提示；
- `git diff --check`通过；
- 本地前端 `http://localhost:5173/`、后端 `http://localhost:8080/` 已重新启动，健康检查和随机临时账号实际登录通过；临时密码只保存在当前启动终端，不写入本文件；
- 浏览器实际通过“提质增效—高效沉淀池分析”进入模块，逐页点击分析概览、数据与计算、偏差诊断、优化方案、效果跟踪；数据页确认29项计算结果及29条公式显示，诊断页确认20条规则和原因核验；
- 场景B确认 `HSC-DATA-01` 为数据不足、4条药剂规则被抑制且发起方案按钮禁用；场景C确认 `HSC-DATA-02` 为数据不足、4条HRT/沉淀规则被抑制、1条规则不适用且发起方案按钮禁用；
- 实际跑通 `HSC-CHEM-04` 原因确认、自动带入优化方案、状态推进、效果验证接受、标记不可比禁止固化以及重新满足可比性后恢复；
- 900×800窄屏下页面无整体横向溢出，五页签在自身容器内横向滚动，双栏内容自动转为单栏；浏览器控制台未发现warning或error。

当前限制与后续接入：

- 当前字段、指标、规则结果、原因核验、方案和效果验证均为固定本地示范数据及浏览器 `localStorage`，尚未接后端数据库、25个API、自动取数、权限、审批或审计快照；
- 15项基准中仅设计表面负荷为已确认可发布，其余经验、本厂目标和历史优秀区间仍为待业务确认或草案；示范目标21.5 mg/L、验证目标23.2 mg/L和年化节约仅用于产品链路验证；
- 当前规则执行适配器用于可运行演示，正式后端应按 `CONTROLLED_AST_V1`、规则执行契约、160个测试场景和模型发布规范实现，不应把前端演示状态映射直接当作生产规则引擎；
- 本地预览进程结束、电脑休眠或重启后需要重新启动，后端每次启动都会生成新的临时密码。

### 18.20 提质增效—V型滤池分析 V1.0（本地首版）

2026-08-31 已按 `/Users/fen/Documents/WaterX工艺管理/docs/提质增效/V型滤池设计包_V1.0/` 及配套业务确认工作簿，将“V型滤池分析”作为提质增效的第二个标准化单体模块接入主产品。本轮仅修改本地并预览，没有创建Git提交、没有推送GitHub、没有部署CDS；开始时已核对并保留全部既有未提交修改。

通用化与模块文件：

- 新增 `apps/admin-web/src/modules/unit-analysis/` 通用单体层：`model-registry.ts` 将首个HSC专用模型入口泛化为 `getImprovementModelVersion(modelCode, versionNo)`，同时保留 `getHscModelVersion` 兼容代理；`UnitAnalysisTabs.vue` 为HSC和VF共用五页签母版；`state-machine.ts` 统一方案推进、回退和效果验证状态机；
- 高效沉淀池 `HighEfficiencySedimentationPage.vue` 已改用上述共用页签与状态机，不改动其原有业务数据、规则和页面内容；
- 新增 `apps/admin-web/src/modules/v-filter-analysis/`：`vf-model-0.1.0.json`、`types.ts`、`demo-data.ts`、`engine.ts`、`adapter.ts`、`acceptance.ts`、`validate-acceptance.mjs`和 `VFilterAnalysisPage.vue`；
- `apps/admin-web/src/App.vue` 在“提质增效”下保留“高效沉淀池分析”并新增“V型滤池分析”入口，既有一级导航顺序和其他模块不变。

实现范围：

- 原样纳入已确认的VF机器模型，数量固定为84字段、35指标、20候选基准、24规则、30原因核验和20页面组件；模型状态保持 `DRAFT`，20项基准全部显示“待项目确认/仅演示试算”，未发布为正式生产阈值；
- 完成分析概览、数据与计算、偏差诊断、优化方案、效果跟踪五页签；数据页完整展示84个字段及35个受控计算结果、公式、来源、覆盖率、质量状态和单体边界；
- 气水同时反洗仅使用单一 `combined_duration`；固定演示事件同时保留流量、台数、阶段时长、有效面积、事件编号和程序版本；滤前后样本可比性明确核验点位、时间、水力时滞和检测方法；
- 固定且可重置的四个场景为“水质稳定但反洗水耗偏高”“反洗事件证据不足”“滤前后样本不可比”“分格数据不足”，状态统一为 `NOT_APPLICABLE / DATA_INSUFFICIENT / NORMAL / TRIGGERED / SUPPRESSED`；
- `VF-DATA-01`、`VF-DATA-02`、`VF-DATA-03` 命中时只产生补数要求，不生成业务偏差或优化方案；三条数据规则分别抑制反洗/水耗/能耗、局部效果、分格/滤料/配水依赖规则，并明确显示抑制来源；未配置表扫时先按适用性返回“不适用”；
- 原因核验支持待核验、已确认、已排除、证据不足和不适用；业务规则可自动带入来源、证据、目标、步骤、守护指标、停止条件和回退条件形成受控方案；
- 效果跟踪比较基线期与试验期的反洗水率、吨水反洗电耗、滤后浊度和过滤周期；标记不可比后清空验证收益并禁止固化，恢复全部可比检查后才能继续认定。

自动与浏览器验证：

- `validate-acceptance.mjs` 实际执行通过，24条规则每条覆盖不适用、数据不足、正常、临界、触发、持续不足、恢复和被抑制8类合同场景，共192/192通过；同时校验84/35/20/24/30/20数量、边界完整性、数据规则返回状态和未确认基准发布保护；
- 管理端 `vue-tsc --noEmit` 通过；Vite生产构建通过，130个模块完成转换，仅保留既有大包体积提示；`git diff --check` 通过；
- 使用新生成的本地临时账号登录成功，实际打开“提质增效—V型滤池分析”，点击五页签、核对84字段和35指标、查看24条诊断与30项原因核验，完成原因确认、发起方案、状态推进、不可比阻断和恢复验证；
- 场景B确认 `VF-DATA-01` 返回数据不足，当前无表扫配置的 `VF-BW-08` 返回不适用，其余10条适用业务规则被抑制，发起方案按钮禁用；场景C确认 `VF-DATA-02` 抑制3条局部效果规则；场景D确认 `VF-DATA-03` 抑制3条分格/滤料/配水规则；
- 高效沉淀池共用五页签回归正常；884×768窄屏下没有页面级横向溢出；浏览器控制台未发现warning或error。

当前限制与预览状态：

- 当前是本地可运行演示适配层，尚未接入后端数据库、自动取数、通用单体API、审批权限或不可变审计快照；正式后端应使用同一 `{modelCode}` 入口和 `CONTROLLED_AST_V1`，不应将前端演示状态映射当作生产规则引擎；
- 20项候选基准及采样、滤格编码、水头、反洗程序、能耗计量、水成本、安全边界和初滤水等试点口径仍待业务冻结，模型不得从 `DRAFT` 转为生产发布状态；
- 当前管理端 `http://localhost:5173/`、后端 `http://localhost:8080/` 已重新启动并登录验证通过；临时密码仅保存在当前启动终端，不写入HANDOFF。
