# ADR-0001：一期采用模块化单体

状态：已接受

一期采用 Spring Boot 模块化单体，不拆微服务。风险、检查、隐患、证书和作业票按领域包隔离，共用同一 PostgreSQL 事务边界。

原因：一期核心难点是业务规则、权限和闭环一致性。模块化单体更容易保证检查异常转隐患、跨模块审计和版本快照的一致性，也降低部署与运维成本。领域边界和 API 保持清晰，未来存在独立扩缩容需求时再拆分服务。
