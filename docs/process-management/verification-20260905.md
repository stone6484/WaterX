# 工艺管理 MVP 首批本地验收记录

日期：2026-09-05；真实仓库 `/Users/fen/Documents/0-AIX/WaterX工艺管理`；未提交、未推送、未部署。

## 1. 实际改动

- 新模块：`apps/admin-web/src/modules/process-management/` 内的 `types.ts`、`engine.ts`、`adapter.ts`、`MetricEditor.vue`、`DiagnosisBoards.vue`、`ProcessManagementPage.vue`、`process-management.css`、`verify.mjs`。
- `App.vue` 只做五页面挂载、既有指标目录/配置适配、读取登录身份和取消旧日记录自动种子；旧模板和旧数据保留。
- `packages/api-client/src/index.ts` 复用既有 `/v1/auth/me` 读取当前用户，供本地保存记录注明操作者。未新增后端端点、数据库或权限策略。
- 业务规则文档、本文及根 HANDOFF 同步。其他 UI 组件、登录页、管理质量及 UI 规范文件存在并行修改，不归入本批业务改动，不覆盖。

## 2. 自动检查

- 管理端 `typecheck` 和 `build` 通过；生产构建 181 个模块，保留既有大分块警告。
- `pnpm --filter @safety/admin-web check:ui` 通过。
- `git diff --check` 通过；未跟踪的 10 个本批文件另外执行 `git diff --no-index --check /dev/null <文件>`，无空白问题。
- `node apps/admin-web/src/modules/process-management/verify.mjs`：32 项通过，直接转译加载真实 TypeScript 引擎和适配层，不复制业务实现。
- 用例覆盖：真实 0、缺失/异常/不适用、完整数值解析、除零、非正设计依赖、HRT/气水比/商品药耗单位、上限/下限/区间/零目标、无工况、区间重叠、未来设计、跨线设计隔离、同日唯一及修订、来源过期、锁定更正、旧快照不变、禁止删除已引用记录、项目隔离、并发版本冲突、存储失败和损坏档案保护。

## 3. 已实际点击的主产品路径

环境为 `http://127.0.0.1:5173/`，后端 8080；使用既有预览启动器的临时账号实际登录。没有把密码写入文件。

1. 设计页维护并保存一期示范设计 V1；工况页保存启用示范目标 V1。
2. 运行数据 2026-09-05 保存完整示范；生成 138 项诊断，并以 V1→V2 验证同日列表仍只有一条；锁定→原因更正形成 V3，后续公式口径修订形成 V4。
3. V4 为 PM-0.1.1，110 项数据有效；正常 64、预警 31、告警 8、未判定 28、仅参考 7。均为示范校核结果，不表示现场异常。
4. 2026-09-06 为专用于跨日期验收的缺失样例，不是真实未来运行记录。进水 COD 手工填写真实 0，仍显示 0；缺失 TN/水量等不回退演示值，相关计算保持未判定。
5. 缺失样例运行 V2 将 TN 标为数据异常、色度标为工艺不适用并说明验收原因。已保存诊断 V1 仍引用运行 V1；页面提示来源更新，经“更新诊断→保存”后诊断 V2 引用运行 V2。
6. 无日数据、无有效工况的二期生成草稿 138 项未判定、0 项正常；保存正式日报被明确拒绝。
7. 复制启用工况造成同线日期重叠，保存被拦截；放弃复制草稿后原列表仍一条。
8. 切到第二示范项目，诊断列表为空；切回第一项目，原两天记录仍在。页面刷新后档案保留，未保存草稿可恢复。
9. 指标规则详情实际可打开；气水比展示同日日累计公式、实际结果和来源版本；来源下钻打开对应运行历史版本。
10. 工艺分析日报打开相同日期/版本，与诊断同一快照且没有更新或保存按钮；历史版本选择可用。

## 4. 视觉与浏览器观察

- 1440×900 和 1280×720 检查无页面级横向溢出；1280 下两看板宽度均 508px，表头/表体实际宽度均约 468.45px。
- 双看板保持独立分类、固定表头与局部滚动；没有把新的业务逻辑堆回 App.vue。
- 本轮检查浏览器控制台未发现新增警告或错误。选择器因热更新/标签计数变化出现过定位失败，重新读取页面后恢复；不计为产品控制台错误。
- 新模块确认提示改为页面内对话框，避免内置浏览器原生确认框无法可靠操作；连续保存增加忙碌保护。

## 5. 未通过或尚未验证

- 点击导出未得到浏览器下载事件，无法确认文件落盘；当前环境没有可用 Chrome。不能声称已验证导出文件内容或模板导入回读。
- 打印按钮未显示可核验的系统打印预览；已经修正旧全局打印样式隐藏内容的问题，但 A4 实际分页、完整 138 项打印和黑白归档仍需普通浏览器复验。
- 本轮未对化验 18 类、过程评价 131 项、安全全部业务重新执行完整回归；没有修改其业务逻辑，类型/构建不等于全业务验收。
- 尚未接自动采集、正式后端持久化、跨人权限/审核与审计。复杂模型与项目目标参数仍待校核；当前仅完成可信的本地日诊断主链，不等于工艺管理生产上线。

## 6. 接手顺序

先复核以上未完成的文件/打印用户路径，再校核 28 项未判定模型和目标口径；在确认来源映射、统计日边界与异常处理后接实际数据和后端。保持旧存储数据，不直接更名指标键；任何发布单独取得授权。
