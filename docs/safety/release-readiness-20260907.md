# 安全管理首批：发布准备清单

2026-09-07。本文件是发布前门槛，不是上线完成证明。用户计划下午发布进入本人业务测试，尚未执行发布。

## 已有证据

- 本地隔离环境完成退回、每轮新证据、独立复查、归档只读的浏览器链路。
- 后端13项、导出8项、工艺40项、全厂分析64项回归及两端构建通过；具体事实见当日验收记录。
- 本轮新增升级恢复1项，后端完整回归最新为14项、0失败/错误/跳过；差异空白检查通过。前端及业务代码未在此轮修改。
- 全部未提交开发保留；本批安全修正没有修改工艺业务数据和规则。

## 发布前必须确认

| 门槛 | 当前状态 | 验收要求 |
|---|---|---|
| 实际打印 | 用户人工验收通过（2026-09-07） | 用户查看已下载的 `YH-20260907-0511234F-整改记录.html` 后确认“可以的，没问题”；本样例的打印与归档展示门槛关闭。此为用户人工确认，不冒称Agent已生成PDF或逐页截图验证，也不外推至所有模板和打印机 |
| 发布目标与自动部署 | 正确目标已核实；自动部署开启 | 用户补充链接后，实际核对 `https://geole.me` 项目 `505739583b45`，名称WaterX智慧水务运营平台、仓库 `stone6484/WaterX`。推送自动创建/构建分支，后端启动执行Flyway；正式提交和发布方案尚未确定，暂不推送 |
| V19旧数据升级 | 带旧数据的隔离演练通过；目标库待验 | 新增测试覆盖五种旧状态×有无责任人10条记录及原种子数据，旧字段与迁移快照、分类、任务/结果/附件不丢失；仍须使用目标库安全副本核验，不能以合成样例代替实际存量数据 |
| 数据备份与恢复 | 本地冷备恢复通过；CDS备份尝试失败 | 用户批准后实际发起一次主库手动备份，CDS因 `/data` 文件系统只读返回500，未取得成功备份记录；独立恢复及实库V19演练未执行。本地PostgreSQL 14.22样例不证明线上16.14可恢复 |
| 真实身份权限 | 等待已启动的角色设计确认 | 用户正在专项讨论人员决策和权限，后续按角色试用；不擅自配置真实账号或固定新方案。落实前核对厂区/人员关联与整改复查分离 |
| 附件与签字留存 | 持久化配置已核实；实际恢复待验 | CDS配置声明 `FILE_STORAGE_DIR=/data/uploads`、`waterx_uploads:/data/uploads`，PostgreSQL声明 `waterx_postgres:/var/lib/postgresql/data`。尚未取得运行容器挂载检查、备份记录及跨重启下载证据；卷存在的配置不是备份证明 |
| 发布后验收 | 未执行 | 真实入口登录、上报→派发→接收→整改→复查/退回→归档、附件下载、历史版本、退出再登录及工艺主要页面回归 |

## 业务测试注意

- 工艺、管理质量、过程评价、提质增效等部分新模块仍使用浏览器本地档案；换浏览器/域名/端口不会自动迁移记录。开始实际业务测试前备份现有档案，不输入唯一一份不可恢复的业务资料。
- 5183/5184连接临时8082，后端退出后测试数据不保留；5173/8080是旧预览，不能作为新版安全后端发布证据。
- 本批不把危险作业票、归档后追加更正、综合检查动态多问题或外部通知标为完成。
- 不在清单、HANDOFF、提交、PR或截图中记录临时密码、管理凭据或令牌。

只有取得上述证据并获明确发布授权，才进行GitHub/PR/部署步骤；当前不更改生产配置或真实数据库。

## 本轮预检证据与后续执行条件（2026-09-07）

- 新测试：`backend/src/test/java/com/waterx/safety/SafetyMigrationRecoveryTest.java`。使用JUnit独占临时目录、随机本地端口，不接受真实数据库连接；V18及V19恢复均成功，迁移再次启动执行0次，未重复导入历史。首次演练发现备份复制未保留目录权限，已修正复制时保留属性，复跑通过。所有试验数据随测试清理，不是可用于生产恢复的备份。
- 恢复样例只覆盖相同数据库二进制/本机架构、无外部表空间或符号链接的冷备；不能直接当作线上操作脚本。生产环境应按其实际数据库版本和CDS登记能力制定方案。
- 源码确认：隐患附件正文存于 `FILE_STORAGE_DIR` 指定目录（默认相对路径 `./data/uploads`），数据库保存附件索引，手写签署及历史签署在数据库记录/快照中。只备份数据库不会保全照片附件。现有本地Compose仅包含PostgreSQL/Redis/MinIO；不能据此证明部署后的应用附件目录已有持久卷。
- 项目工具现状：本机CDS CLI版本0.8.0，manifest缺失，认证只有 `check`、没有 `inspect --strict`；旧 `update` 实现会改用户级技能目录，与项目级更新约束不符，未执行。此次仅通过正常浏览器登录只读核实目标身份，未申请、扩大或批准Agent权限，未读取环境变量值。
- 正确目标已按用户补充链接核实（见下一节）；此前错误目标 `cds.miduo.org/f9e8b956d3dd` 不再用于WaterX操作。正式操作前仍需记录目标数据库版本及迁移版本、备份时间/保留位置/恢复验证、附件目录与实际持久卷、服务和最终提交、恢复点、预计影响；维护窗口或配置修改集中请求批准。

## 正确CDS项目只读核验（2026-09-07，用户补充链接后）

- 唯一目标：主机 `https://geole.me`，项目 `505739583b45`，slug `waterx-2`，仓库 `https://github.com/stone6484/WaterX.git`。主分支标识 `waterx-2-main`；本地开发分支对应 `waterx-2-codex-release-waterx-20260816`。复用已有浏览器登录会话，未跨主机传递凭据。
- 当前页面报告main运行、应用服务3/3，上线入口 `https://main-waterx-2.geole.me`。部署面板短提交为 `fdab8c9`，最近部署为北京时间2026-09-04 01:33:18；开发分支显示 `f90db40`、idle及关联PR #7。此次未读取GitHub确认PR状态和完整提交，不能用旧HANDOFF的PR #6/合并哈希代替当前实际部署身份。
- 项目基础信息明确“自动部署开启”“push时自动建分支＋构建”。项目共享配置让后端连接项目级PostgreSQL；尚无分支数据库隔离证据，不得将“新分支”自动等同“独立测试库”。先克隆并确认连接隔离，再启动包含V19的新后端。
- CDS已批准配置v2（2026-08-15）声明上传卷、数据库卷；基础设施页PostgreSQL 16显示运行。当前后端容器启动日志（2026-09-04 01:32）显示实际PostgreSQL 16.14、已验证18个迁移、schema版本18。它是启动时证据，不是本轮实时SQL读取；此前本地14.22演练不能替代线上16.14同版本恢复验证。
- 存储页持续显示“加载存储信息”；数据库资源的备份页及一次列表刷新仍显示“读取备份列表”，未返回可核验备份文件、时间或恢复结果。不能解释为“备份列表为空”，也不能标记恢复门槛通过。未点击手动备份、克隆、创建数据库、连接已有、重启、停止、升级CDS或部署。
- 本轮只读配置核查，无生产变更、无新角色配置、无本地业务代码修改。后续待批准的最小执行范围：在CDS内制作数据库与附件成套备份、恢复到独立测试目标并核对行数/附件/历史，再演练V19；不覆盖主库、不停主服务、不对外导出业务数据。若CDS功能或权限不能完成，应交由运维确认能力，不通过手写旁路请求绕过。

## 用户批准后的备份执行结果（2026-09-07）

- 用户已批准上一节最小执行范围，不需要重复申请同一备份操作；仍未授权修改生产挂载/权限、覆盖主库、停止服务、发布或配置真实人员。
- 再次锁定 `https://geole.me` / `505739583b45` / `waterx-2-main`，页面仍报告main短提交 `fdab8c9`、应用3/3运行。在该分支PostgreSQL资源的备份页实际点击一次“手动备份”。
- 备份列表读取与手动备份均返回HTTP 500，错误为 `mkdir: cannot create directory ‘/data’: Read-only file system`。手动备份请求标识 `9206fcfa`，列表请求标识 `c87da723`；资源为 `infra:postgres`。未取得备份文件、大小、完成时间或成功任务证据，不将失败说成备份完成或没有历史备份。
- 已停止依赖该备份的独立恢复和V19演练。没有执行克隆、空库创建、连接改写、恢复覆盖、数据库迁移、停止/重启、Git提交/推送或CDS发布；附件成套备份也尚未完成。未修改CDS系统挂载、权限或认证策略来绕过失败。
- 需CDS维护同事核查备份处理程序中 `/data` 的实际归属、挂载及写入条件，并验证备份列表和创建备份恢复正常；这不等于授权把整个容器或主库目录改为可写。平台修复后，先取得数据库和附件成套备份，再确认独立恢复目标和连接隔离，最后开展V19演练。
- 本地发现其他任务新增V20工艺日数据协作迁移及相关代码，全部保留，不纳入本轮安全发布范围。安全恢复测试的升级和校验显式限定 `target("19")`，避免随工作区新增迁移改变演练范围；本次定向复跑1项通过，0失败/错误/跳过。之前14项全量通过为上一轮结果，不宣称新增V20已通过本轮全量回归。
