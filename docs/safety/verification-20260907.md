# 安全管理首批本地验收记录

日期：2026-09-07。开发阶段已开始，本记录为已执行的工程和页面验证，不代表业务专家已完成验收或已发布。所有操作使用隔离测试数据；已有5173/8080预览与数据库未升级。

## 已验证

- 后端全部13项测试通过，0失败/0错误：新安全工作流9项、既有安全API综合测试1项、身份令牌1项、风险计算2项。覆盖真实厂区角色、本人操作、四级与法定认定分离、跨厂拒绝、并发版本冲突、重复上报、每轮新证据、本人不得复查、退回重提、关闭不可改、附件内容下载、模板新旧隔离、条件题与反向题、不适用理由及旧内容清除、重复问题关联、周期计划重复生成和暂停恢复。
- 管理端与手机端类型检查/生产构建通过；末次手机首页待办修改单独再构建。既有管理端体积警告仍在，未为本批做无关分包。
- UI静态守卫通过：40个Vue文件、令牌、16个固定图标、6个冻结语义色与14处风险色绑定。工作区git diff --check通过。
- 实际浏览器登录：安全员管理端、责任人手机端；身份显示来自实际账号。模板库9类131项与3份历史示例分别可见。
- 实际浏览器操作：现场上报文字草稿→关闭保存→恢复→提交待受理→安全员分类/等级/部门责任人派发→手机首页显示待接收→责任人填写意见接收→变为整改中。版本1/2/3及三次处理历史均可见；专业字段未被上报时自动代填。
- 布局：管理端实际1280宽度的模板库页签和卡片已检查；手机390宽度详情无整页横溢出。验收时两端控制台未记录错误。不是全模块全尺寸覆盖。

## 本次实际修正

共享页签被历史导航样式影响的颜色/竖排问题；模板卡片间距；隐患操作草稿不能恢复；关闭/取消时未保存内容保护；手机首页漏显责任接收与整改待办；未读取风险资料误显示为0；顶部办理人身份写死为平台管理员；检查改为不适用后遗留旧问题。

## 未宣称完成的验证

手机实际上传与手写签字完整链路、桌面实际打印分页/下载文件人工核对、所有长模板现场填写耗时、真实项目数据迁移及真实员工权限配置尚需现场试用补验。后端已自动验证签字必需、证据上传/下载、整改退回和独立复查规则，但不能据此声称所有手机浏览器兼容性已验收。

归档后当前只读，无追加更正入口；综合单采用一个开放记录项，多个独立问题通过独立上报处理。完整危险作业与普通维修分流为下一批；原作业票的历史问题未因本批完成而消失。

## 发布和接手

代码、规则与来源映射已在主产品同一工作目录，无需从设计目录复制。2026-09-07用户已授权交接至主产品开发阵地，现已接手并停止原任务并行代码修改。未提交、推送、部署。临时验收服务只用于本地；进程退出会清除临时数据库。业务评审优先核对流程、责任边界和原表含义，再安排危险作业批次。

续接标准：业务与UI同批开发、同批验收；按最新已确认规范补验管理端与手机端完整办理链路，发现问题在本批修正。上文13项测试及页面结果属于交接前证据，续接实际结果另行追加，不借历史证据宣称新增验收完成。

## 主产品续接实测（2026-09-07 11:30后）

本节更新上文尚未补验部分；不意味着所有手机设备、打印分页或正式数据升级已通过。

- 使用隔离8082数据，实际浏览器完成同一演示隐患 `YH-20260907-0511234F`：责任人首次上传演示附件和笔迹提交V4 → 独立复查人退回V5 → 责任人仅用旧附件提交被明确拦截（“请上传本轮整改照片或凭证后再提交”）→ 补传本轮演示附件、笔迹后提交V6 → 独立复查人通过并归档V7。最终只有导出/查看，无再次派发、整改、补附件按钮。9次事件、两轮整改、一次退回及最后归档均保留。
- 本测试用公开Logo图片及鼠标绘制测试笔迹，不是实际隐患照片、个人真实签字或安全合格结论。责任人和复查人使用不同测试身份；不宣称第三方电子签章效力。
- 实际下载整改记录HTML并回读：57,403字节（当次下载），9段事件、4幅逐次签署、V7、两轮意见、退回记录和附件索引齐全。新增8项真实导出函数测试覆盖历史签署保留、中文状态、费用0/缺失、HTML转义、CSV公式防护（含前导空白）、原始回答及模板来源、打印样式契约。
- 修复：长模板编辑器原来缺白底/高度边界，已改为独立白底滚动窗口；在1280×720实测37项全部存在、末项可达，窗口680×688，标题保持可见。底部关闭时未保存提示回到顶部可见位置，放弃测试草稿后没有发布模板。模板版本升级隔离已有自动测试通过，本次没有修改9类原表业务文字。
- 修复：安全详情抽屉独立滚动，1280×720下末次归档事件可见、标题顶部固定，背景模态层保持全屏；不改管理质量抽屉。修正安全页签顶部强调/表格细线、费用单位、手机按钮作用范围及重复成功提示；签字模型清空时同步清空画布。
- 工程复验：后端13项测试重新通过，0失败/错误；工艺40项、全厂分析64项原有回归通过；导出8项通过；UI守卫40个Vue文件通过。两端构建曾因沙箱禁止写构建缓存失败，授权本地写入后两端构建通过；原有管理端大分块警告保留。最终代码构建及差异检查在本节记录后再次执行。
- 当前两端浏览器错误日志回读为空；“无本轮证据”是主动测试得到的预期业务拒绝，不是可绕过的错误。

### 仍未通过的发布前核验

1. 打印门槛已由用户人工验收关闭（2026-09-07）：用户查看已下载的 `YH-20260907-0511234F-整改记录.html` 后明确回复“可以的，没问题”。此前Agent因浏览器策略不能打开本地文件，未绕过限制；此次通过依据为用户确认，不是Agent生成PDF或逐页截图验证，不外推至其他模板和打印机。
2. 真实存量数据库升级及恢复演练、真实员工业务权限、附件持久化及部署联动检查未做；仅临时数据库的自动迁移/流程测试通过。原5173/8080服务未重启，原数据库未迁移；前端构建产物会更新，但不代表该旧后端支持本轮安全功能。
3. 长表实厂填写耗时、真实手机兼容性继续试用校核；归档后追加更正及综合检查动态多问题、危险作业深化仍为下一批。未宣称本轮安全功能已可无条件正式上线。

发布门槛与操作边界见 [发布准备清单](release-readiness-20260907.md)。未提交、推送、创建PR或部署CDS。

## 发布前隔离升级与恢复演练（2026-09-07 14:00后）

- 新增 `SafetyMigrationRecoveryTest`：先迁移至V18，构造五种旧隐患状态×有无责任人共10条额外样例（含零费用、旧完成/复查意见），另有既有种子记录、完成任务检查结果及附件索引；升级V19后逐字段核对历史快照，核对状态映射、旧分类、缺失签署不伪造、任务/结果/附件保留、9类131项模板和再次启动不重复迁移。
- 使用干净停机的隔离PostgreSQL 14.22目录分别制作V18、V19冷备，复制到新的恢复目录并启动；比对所有public表的完整JSON行集合，V19新增演练内容正确保留，演示附件恢复后字节一致。首次复制未保留目录权限导致恢复启动拒绝，已在测试复制逻辑中保留文件属性，重跑通过；没有弱化数据库权限要求。
- 后端完整回归最新结果：14项，0失败、0错误、0跳过（新增恢复1项＋原13项）。`git diff --check`通过。本轮只新增测试和更新交接/验收文档，未改产品业务代码、前端或V19迁移脚本，未重新宣称全站浏览器验收。
- 边界：这是合成样例、相同数据库二进制下的本地冷备，不是线上库副本、热备、跨版本恢复或真实附件跨部署验收；测试目录由JUnit清理，不作为正式备份交付。
- 线上只读核对发现目标冲突：此前 `cds.miduo.org` 的 `f9e8b956d3dd` 实为 CDS Self（非WaterX仓库），当前项目列表未见WaterX。未修改或申请任何项目权限、配置、数据和部署；等正确WaterX项目设置页链接后继续。人员试用权限按用户正在专项讨论的角色方案另行确认。

## 用户补充正确CDS入口后的只读核验（2026-09-07）

上节目标冲突已解决：实际核实 `geole.me/505739583b45` 为WaterX项目及正确仓库，main显示3/3运行，自动部署开启。CDS批准配置声明数据库及上传附件两个持久卷；后端上次启动日志为PostgreSQL 16.14/schema V18。存储页和备份列表仍未返回可核验备份记录，不能认定无备份或恢复通过；真实库备份、独立恢复/升级、附件跨重启与分支隔离仍待确认。详情及准确验证边界见发布准备清单。此轮仅更新文档并做差异检查，不改业务代码，不提交、推送或部署。

## 用户批准后的实际备份尝试（2026-09-07）

- 正常CDS页面已实际发起一次WaterX主分支PostgreSQL手动备份；列表读取及创建备份均返回HTTP 500：`mkdir: cannot create directory ‘/data’: Read-only file system`。创建请求标识 `9206fcfa`，列表标识 `c87da723`。没有成功备份证据，不能继续宣称恢复或升级门槛通过。
- 已停止后续独立恢复/升级，不覆盖或重启主库、不发布、不改CDS挂载/权限。附件备份未完成。需平台维护同事先核查备份目录挂载及写入条件；完整执行边界和续接顺序见发布准备清单新增章节。
- 保留其他任务新增的V20工艺日数据协作代码；本轮仅将安全恢复测试两处Flyway最新版本调用明确限定V19，未改业务代码或V19/V20迁移。定向运行 `SafetyMigrationRecoveryTest`：1项、0失败、0错误、0跳过，耗时4.191秒。此结果不覆盖V20，也不替代真实PostgreSQL 16.14副本演练。
