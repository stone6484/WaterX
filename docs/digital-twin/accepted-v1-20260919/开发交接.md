# 数字孪生首批开发交接 · 2026-09-19

## 状态与授权

用户已要求将本轮数字孪生设计推送给“主产品开发阵地”继续开发。当前版本可作为首批交互与视觉开发基线；这不是工程图、真实厂站模型、正式监控或上线验收。

接收任务：主产品开发阵地（01a041c9-8e15-7283-a5e6-6b806abdf980）。真实代码仓库：`/Users/fen/Documents/0-AIX/WaterX工艺管理`。先读该仓库 HANDOFF.md、相关 UI 规范与当前 Git；保留驾驶舱、工作台及其他已有未提交修改。本设计任务不并行修改主产品代码。

当前授权：在主产品任务中开展本地开发、测试和预览。不是 GitHub push、PR 合并、CDS 部署或真实生产控制授权。

## 交付物与启动

本文件所在目录是固定快照。index.html、app.js、style.css 是运行原型；case-data.json/data.js 是同源假想案例，vendor 内含 Three.js 及 MIT 许可。manifest.sha256.json 记录运行文件哈希。README.md 与 scenario.html 为场景说明及公开形态参考。

原型源目录：`/Users/fen/Documents/WaterX工艺管理/outputs/waterx_digital_twin_demo_20260919/`。当前本机预览为 http://127.0.0.1:8877/；它依赖本地进程，不是可对外访问的地址。固定快照可单独运行：在本目录执行 `python3 -m http.server 8878 --bind 127.0.0.1` 后访问 http://127.0.0.1:8878/ 。本次仅提供启动方法，未启动第二服务。

原目录的 preview-*.png、verification*.json 是历次阶段证据，部分早于最新菜单与悬浮布局；不得将旧截图当成当前要求。本目录源码和以下最新交互约定优先。

## 已确定的首批范围

- 单厂示范：假想澄川污水厂，10 万 m³/d、地上式经典 A²/O，25 个设施、96 个设备、547 个模拟信号、45 条示意流路。平面总图、四组生化池、二沉池、预处理、深度处理消毒、污泥及辅助设施均为案例假设，不需再要求用户提供实厂图纸才能做首批。
- 保留三维总览、平面视图、地面漫游、七站巡检、回正、剖切/去顶、名称/读数显示、对象选择与业务详情。模型细节包括风机、泵、管道法兰、混凝土和活性污泥混合液/气泡；这是程序简模与视觉动画，不是精模或生化计算。
- 顶部操作顺序：三维总览、平面视图、地面漫游、巡检路线、一键回正、剖切/去顶、名称。右侧为“选择”：预处理、生化处理、二沉及回流、深度处理与消毒、污泥处理、辅助设施、流路图层。窄屏可自然换行。
- 工艺导航悬停/点击下拉，展示设施与所属设备名称，不显示内部编码（例如 CLR-01）；对象名称中区分不同池/机组的序号仍保留。点击对象定位并更新资料。不要恢复左侧对象导航栏。
- 流路图层在“辅助设施”之后的下拉菜单，可多选水、回流、空气、污泥/回水、加药；选中一项不关闭菜单；默认水图层开启。不要恢复右下角图层卡。
- 风险面板左上、对象资料右上，都在三维视图区内、工具栏下方，默认展开并固定。统一 16px 顶/侧边距、圆角与阴影；固定只保持展开，不挤压三维场景宽度。支持收起、取消固定、移出自动收起；键盘焦点在面板内时保留。
- 风险收起后为左上侧报警灯悬浮球，点击可展开。任意告警为红；没有告警但有预警为黄；无事件为绿。文字/无障碍名称同时说明状态，不仅依赖颜色。无风险只代表当前数据源没有触发事件，不等于厂区安全证明。右侧收起后保留“资料”入口。
- 关键数值直接标注在相应设施设备附近，以引线对应、随视角更新并避让面板及彼此；不恢复独立关键读数卡。生化池含 DO、MLSS、厌氧/缺氧 ORP、进水流量、氨氮；进出水含流量、pH、COD、氨氮、TN/TP 等；设备近景含运行状态、频率、电流/压力/温度/振动。拥挤时优先选中对象与主要工艺数据。
- 标签覆盖不能阻断场景滚轮：鼠标在池体/设备/数据标签和空间风险标记上仍能缩放；面板内容独立滚动，不应带动场景缩放。此缺陷已在最新 app.js 修复。
- 风险与信号/场景标注/回放使用同一计算来源。离线或缺失不可显示为 0 或正常。示例情景含风机故障、温度预警、H₂S 报警、DO 离线；阈值 75°C、10ppm 仅为演示设定，不可变成生产默认限值。
- 回正恢复居中顶视、关闭剖切、显示名称和双侧默认面板、退出巡检；保留当前情景/回放时刻和已保存示例巡检记录。不要恢复此前删除的操作提示横幅。

## 主产品接入顺序与边界

1. 在既有 Vue 管理端新增独立数字孪生页面，复用主产品壳、当前项目上下文、共享样式和权限约定；明确示范场景身份，不把澄川模拟厂伪装成当前项目。入口位置按主产品现有导航组织，用户并未指定新的全局导航体系。
2. 把原型内场景创建、标签投影、图层、面板状态及模拟数据适配到页面生命周期；不要直接整页 iframe 嵌入第二套 WaterX 头部。可复用 Three.js 与几何生成代码，进入/退出页面需释放动画、观察器、事件监听、纹理与几何资源。
3. 保留独立模拟数据提供方式，预留厂区/设施/设备/测点稳定 ID 映射。接已有真实业务入口时先核对权限、接口和对应对象；没有对应关系时明确不可用，不伪造跳转成功。不得在真实接口出错、无权限或无数据时静默回退模拟。
4. 首批先完成可运行的本地示范页面与主产品导航集成，不以等待实厂模型、全新物联网后端、数字孪生平台架构为阻碍。实际设备台账、监测数据和正式巡检写入逐项按当前接口条件接入并记录未接项；模拟与真实状态要可辨认。
5. 实厂 BIM/CAD、精确地下管线/水力高程、完整碰撞/室内漫游、生产模型校准、正式告警处置及设备控制不属于本次设计交接已完成能力。不自动下发任何曝气、回流、加药或设备启停指令。

原型巡检使用本地 localStorage，仅可称示例记录；不要直接用全局演示存储键保存跨租户/项目的正式记录。空间光效不代表真实数据质量。静态对象渲染与业务数据接入需分开验证。

## 本地验证证据与接收验收

此前浏览器已逐项检查：顶部设施设备选择、双侧悬浮面板收起/固定/非固定自动收起、红黄绿灯优先级、流路多选及独立取消、读数与回放同步、离线缺失、标签/引线一起隐藏。

最新标签滚轮修复实测：鼠标位于“1 组 A²/O 生化池”读数标签上下滚轮，投影位置随相机变化且页面 scrollY 保持 0；在资料栏滚动时内容 scrollTop 0→69.5，场景投影未变。最新语法检查通过，检查范围内浏览器无 console error。这些是独立原型证据，不是主产品或后端通过证明。

接收方需在主产品重新验证：

- 现有页面/工作台功能不回退，进入退出孪生页无重复画布、监听或动画；无 WebGL 时提示明确，导航仍可用。
- 在常用桌面及窄窗口检查菜单可达、遮挡、面板滚动、缩放/点击标签、剖切近景、漫游和回正。
- 水/回流/空气可同时显示，再分别关闭；菜单内勾选不误关菜单。
- 风险红黄绿三态、离线与真实零值区分、模拟标记、回放一致；场景标记不因风险列表收起而消失。
- 工艺/对象选择能对应到正确设备；只读业务跳转遵循既有项目与权限，不复制造成另一套正式业务记录。
- 类型检查、构建、UI 守卫和受影响回归，浏览器结果及未接接口写入主产品验收记录，更新 HANDOFF 与 docs/ui 入口。

完成首批后提供主产品本地预览与实际完成/未接清单。真实参数工程核定、现场映射、跨浏览器/GPU性能与生产验收仍需后续开展；本次未宣称这些已完成。
