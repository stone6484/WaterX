# 日数据人员协作首批（2026-09-07）

> 第二批最新状态：工艺参数、确认日数据、分析日报版本闭环已完成本地隔离验证，见 [第二批实施与验收](batch2-process-archive-20260907.md)。本页下文保留首批的历史边界，第二批只在独立5186/8084试点开启。当前MVP以工艺必需功能为主，安全按需优化，其他规划模块暂停。

主产品已接手本首批，并补上未保存离开保护、旧请求隔离和读取失败重试；本轮24项后端集合、40项工艺及7项接入回归通过，真实页面填报/提交与独立账号确认通过。完整边界及下一批V21在途问题见 [主产品首批整合验收](batch1-integration-20260907.md)，不要把首批结论外推为完整参数/诊断/日报后端已完成。

用户已确认“执行人员—专业经理—管理层”三层职责，授权建立真实后端协作闭环，仅在隔离本地环境验证。业务方案、人员责任表和权限矩阵位于设计工作区：`/Users/fen/Documents/WaterX工艺管理/docs/人员权限/`；完整实施记录见该目录`首批日数据协作_实施与本地验收.md`。

本批实现分派/改派、本人填报、提交、独立审核退回/确认、更正重审、终止候选、不可变确认版本与办理历史。服务端从同一笔有效角色范围授权核对操作、厂和工艺线；自然员工身份防自审，记录修订号及行锁防并发覆盖。管理层默认查看确认版，不默认取得执行、审核或导出权。日数据确认表示事实记录经过复核，不表示数据完整或指标达标。

## 实现位置

- `backend/src/main/java/com/waterx/safety/process/DailyDataService.java`：逐次鉴权、状态变更、版本和审计事务。
- `backend/src/main/java/com/waterx/safety/process/DailyDataController.java`：`/api/v1/process/daily`接口。
- `backend/src/main/resources/db/migration/V20__process_daily_collaboration.sql`：工艺线范围补充、日数据、确认版本和办理事件。迁移只创建角色/操作定义，不建立人员授权或试点业务数据。
- `apps/admin-web/src/modules/process-management/DailyCollaborationPage.vue`：复用既有原始指标编辑组件；原App导航与共享API客户端小范围接入。
- `apps/admin-web/vite.daily-preview.config.ts`：独立5185→8083本机预览配置。默认5173配置及既有浏览器模式保持原样。前端模式开关不承担权限校验。
- `backend/src/test/java/com/waterx/safety/DailyDataIntegrationTest.java`：10组真实数据库集成验证；测试启动器`DailyDataPreviewLauncher`负责创建全新临时库与随机测试账号。测试模板从当前默认目录提取153项元数据，其中人工填报67项，没有带入设计/目标/实际样例数值。

## 验证与边界

完整后端24项、工艺40项、管理端常规/协作构建和UI守卫通过。实际登录完成退回重提及更正重审，最终预览服务回读R11/V2、11个事件与2个确认版本，旧值0、新值7且其他66项仍缺失。摘要：[运行证据](daily-live-verification-20260907.json)。1280窗口页面无整页横向溢出，厂长没有填报/审核/默认导出按钮。

当前独立预览：http://127.0.0.1:5185/ 。随机凭据仅存放在本机临时目录`/private/tmp/waterx-personnel-impl/preview-access.json`，不得复制到仓库。临时库重建后账号及验收记录会变化，不用于生产数据保管。

没有重启或升级原8080数据库，没有提交、推送或部署。V20不是正式存量库升级验收。规则目标、专业工单和必要的管理审批留待下一批。协作模式的诊断/日报入口目前仅查询确认来源；待审核或更正中禁止取用新的分析来源。分析日报快照后端保存、来源失效标记和设计/工况参数协作尚未接通；既有浏览器档案没有自动导入或转为确认版。
