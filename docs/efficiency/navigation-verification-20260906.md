# 提质增效分级导航验收

日期：2026-09-06；状态：本地导航批次完成，待用户确认。无提交、推送或部署。

## 范围与文件

- `apps/admin-web/src/modules/efficiency/navigation.ts`：集中入口标识、八单体映射、两专题、五个分类/规划页面说明。
- `EfficiencyNavigation.vue`：二级分组与三级入口、折叠、当前页、焦点；首次单体组收起以先呈现整体分类，跳转到具体单体时自动展开。
- `EfficiencyLandingPage.vue`：分类跳转及明确的规划范围说明，复用WxCard/WxState和已有令牌。
- `App.vue`：替换旧平铺导航、接线新页面；八单体原挂载及业务逻辑保持。
- 根HANDOFF同步现状及限制。

## 工程检查

管理端构建含类型检查通过，190模块，保留原有大分块警告；UI静态守卫通过，36个Vue文件；Git差异检查及新增文件行尾检查通过。未新增依赖、规则阈值、数据库、权限或部署配置。

## 实际浏览器检查

环境：已登录 `http://127.0.0.1:5173/`，内置浏览器，第一污水处理厂（示例）。

1. 初次打开提质增效可见总览、单体分析、系统专题、优化任务、效果台账；系统专题包含全厂水量平衡和全厂能耗与运行效率。
2. 总览四个规划入口与侧栏入口都对应同一页面；规划标签、能力范围及未接入说明明确，不出现伪数据或无效保存按钮。
3. 八类单体均可直接打开原页面；分别点击分析概览、数据与计算、偏差诊断、优化方案、效果跟踪，共40次页签检查；对应侧栏 `aria-current=page`。
4. 手动折叠单体分组后，从总览进入高效沉淀池，分组自动展开并正确选中；分组操作不更改业务数据。
5. 1280与1440宽度总览文档宽度均等于视口，无整页横向溢出；可见导航按钮scrollWidth不超过clientWidth；视觉核对文字、分组线、状态和说明无重叠。
6. 本次页面验证末尾捕获的warning/error列表为空。刷新后使用最新代码，避免旧开发连接未热更新导致误看旧导航。

## 明确不代表已完成

- 总览的真实指标/任务/收益汇总；
- 水量和能耗专题的数据录入、计算及结果存储；
- 统一优化任务与效果台账实体；
- 八单体V2全部公式、基准和来源校准；
- 全站业务回归、移动端导航改造或发布验收。

以上均仍为后续分批工作。此次未修改或重置已有单体样例数据。

## 同日入口名称精简

按用户要求统一去掉冗余“分析”：高效沉淀池、V型滤池、预处理系统、消毒系统、排泥系统、污泥脱水、MBBR填料区、曝气供气系统；专题改为水量平衡、能效系统。仅改同源入口标签及三级按钮左右内边距，页面标题、功能、数据和路由标识不变。1280px浏览器实测十个入口均为34px单行且无水平溢出，已截图核对；类型检查通过。
