# 数字孪生精细化与人员场景 · 开发交接 V2.1

确认日期：2026-09-23（Asia/Shanghai）。用户已确认本轮原型暂时收口，并授权交给“主产品开发阵地”整合开发，验证通过后推送 GitHub、发布新版本，同时保存和更新 handoff。

最终微调：下拉箭头上移3px，仍在文字下方但完整位于按钮边框内；以当前style.css为准。此前未发出的V2保留备查，本V2.1是唯一接收基线。

## 当前事实与范围

此包是设计确认快照，不是主产品整合或新版本发布证明。已存在主产品数字孪生模块，本次应在其上增量整合，不另建一套页面、不覆盖旧有生命周期/卸载清理与项目权限。主产品真实代码：`/Users/fen/Documents/0-AIX/WaterX工艺管理`；本设计工作区不是 Git 仓库。

最新用户授权覆盖“本数字孪生增量整合 → 验证 → GitHub 提交/PR/合并 → 既有发布流程更新新版本”。它取代本模块旧交接中“只本地、不得推送”的限制，不扩大到其他未授权模块，不包括真实设备启停/运行参数控制。仅本地开发的模型算法等无关修改不得搭车提交、发布。

源原型：`/Users/fen/Documents/WaterX工艺管理/outputs/waterx_digital_twin_refined_20260922/`。本包固定在其 `handoff/accepted-v2.1-20260923/`，有完整本地资产及哈希清单。浏览器临时 localStorage 不在包里；新环境会加载预设任务，用户后来保存的两条派单仅在原浏览器。

## 以此为准的最终设计

1. 单厂经典 A²/O 示例：10万m³/d、25设施、96设备/仪表、547模拟信号、45流路。十二类设备形态细化、道路/墙体/金属/草地贴图、水面和活性污泥泡沫、池体/建筑/栏杆细节。材料与许可证见《视觉参考.md》及 textures/vendor。均为示例，不是工程图或实际运行事实。
2. 道路上的长距离管道改至侧边通道，过路段下穿；补充材质、法兰和焊缝。现有原型有道路避让几何检查，不得据此宣称完整工程碰撞/埋深/水力校核通过。
3. 左侧视图操作顺序：三维总览、平面视图、地面漫游、巡检路线、剖切/去顶、隐藏名称/显示名称。不得恢复“一键回正”。
4. 右侧：单体直达 → 人员选择 → 预处理 → 生化处理 → 二沉及回流 → 深度处理与消毒 → 污泥处理 → 辅助设施 → 流路图层 → 全屏/退出全屏。右组紧凑、整体靠右，与隐藏名称间留白。前后文字字体字号字重一致、横向中心对齐，小箭头在文字下方独立定位，展开翻转。桌面12px、窄窗11px；不因箭头挤歪文字。
5. 人员/工艺项悬停或点击下拉，设施和设备名称不显示内部编码。流路下拉多选，五类默认全部开启，勾选不关闭菜单。全屏独立深色按钮，退出金色轮廓，置于最右侧。
6. 风险/对象资料为工具栏下方对称悬浮卡，默认展开固定，可收起。风险球红/黄/绿表示任意告警/仅预警/无触发示例；取消固定后按既有交互收起，不恢复硬侧栏。设施、设备读数在对象附近，标签滚轮继续缩放，资料面板独立滚动。
7. 11个可点击人员实体+姓名标签，覆盖8类业务岗位；9类岗位定义含单列的系统管理员（远程、位置未知，不虚构现场实体）。一人一岗，经理/管理各岗位一人，执行岗位可多人。姓名/任职/位置/打卡均为示例，不能创建或赋权真实人员。
8. 点击姓名/实体/顶部人员选择，定位并默认打开待办卡。右卡人员名册、设施的“此处人员”可访问全部例子。默认待办按截止时间排列，详情折叠；“人员资料”放职责、当日班次与打卡；“已完成”只放近期。示例位置固定2026-09-19 08:00，回放时不能冒充新位置。
9. 派发工单/下达指令是给人员的作业要求，只保存本地示例进入指定人待办，不是发送消息、接单、设备控制或作业许可。示例ID关联、错误截止时间、存储失败、损坏记录保护均须保留。人员场景说明中已写真实定位、任务和权限接入规划。
10. 当前用户可见的“假想”措辞改成“示例”；不可擅自改写其他模块或旧版历史交接原文。当前快照优先于9月19日旧版关于回正/默认仅水层/旧工具栏的约定。

## 主产品整合顺序

- 先读主仓库 HANDOFF.md、适用 AGENTS.md、UI规范与 Git 状态。2026-09-23交接时存在模型算法、App.vue、process-management、API client、HANDOFF和docs/ui等未提交工作，必须保护。按主任务既有习惯使用隔离分支/工作树整合，避免其他本地工作进入发布。
- 目标为 `apps/admin-web/src/modules/digital-twin/` 既有 Vue宿主、runtime、simulation、scene、css，并保留既有异步加载与资源释放。原型 index/preview-shell 只是效果参考，不嵌套第二套 WaterX头部/左栏，不直接把全局 IIFE 替换主产品生命周期。
- 增量移入设备/设施/环境/人员/管线/静态合批等场景能力与本地纹理，核对 URL 基路径、Three版本、加载顺序、状态材质与可点击ID。每次卸载清理新增动画、事件、观察器、纹理/几何和窗口暴露，重进无重复画布/任务回调。
- 人员、任务尽量复用现有稳定ID、项目上下文与已经具备的接口/权限。示例与真实模式明确分开，真实数据缺失/错误/无权不可静默回退示例，不以显示姓名匹配任务。示例记录存储按模式/项目/用户隔离；只读或无办理权限不得向正式接口派发。没有正式接口时保留明确的演示闭环并列为未接，不顺势新建全套IoT/定位/考勤/工单平台。
- 真实人员、IoT、定位、考勤、工单与业务台账的接入程度依主产品当时事实记录。发布示例不代表真实服务接通。不要因缺实厂图纸阻塞示例整合，也不要把示例假定变成生产默认阈值/权限。

## 验证、发布和记录

原型验证见《验证记录.md》；`personnel.test.cjs`为人员规则8组，JavaScript语法已过，浏览器已验人员实体/标签点击、待办/资料/完成、派单刷新保留、个人任务隔离、工具栏及全屏。既有Three旧加载格式警告保留；性能数字仅是本机某视角观测，不是上线性能承诺。

主产品必须重新完成类型/构建/UI守卫和受影响回归、孪生模拟计算与人员测试，并浏览器检查：进入/退出/重进、场景/贴图、选中设备、人员定位、重复姓名ID隔离、未知位置、读数滚轮、风险三态、剖切、五图层、全屏、至少常用桌面与窄窗口布局、权限及模拟/真实边界。

验证通过后只提交本模块及其交接/验收文档。按现有 GitHub PR/CI/合并与 CDS 技能进行发布；复用现有发布目标、账号、数据和配置，不擅自更改认证或做无关数据库迁移。需按当前真实配置验证 CDS，而不是盲用旧日志值。最终回读 PR、合并SHA、镜像/构建SHA、部署记录、线上入口与浏览器关键路径，不以服务running代替发布验收。

接手时在主产品 HANDOFF.md 顶部补“数字孪生V2.1接收/进行中”；完成时更新该文件、docs/ui入口、模块验收及发布记录，并把收口入口同步到源原型 HANDOFF.md（快照不改）。记录：实际改动、测试结果、未接项、PR与SHA、线上版本、失败/阻碍和下一步。禁止写入凭据。

## 后续继续

读取源原型 HANDOFF.md → 主产品 HANDOFF.md 最新本模块状态 → 此固定基线和人员场景说明 → 当前代码/Git/线上事实。设计本轮收口不等于功能永久结束，不需重新询问已确认的示例布局、岗位或工具栏约定。
