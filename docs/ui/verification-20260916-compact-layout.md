# 2026-09-16 紧凑布局同步验收（本地）

## 目标与原因

UI 1.0未变。此前只精简了后端协作页面，默认本地档案页面仍保留独立的工艺线/辅助操作行；上一轮虽合并日期筛选，仍未合并共用工具栏。本次同步两套页面的呈现规则，不合并数据模型、不切换正式入口。

## 实现范围

- `ProcessManagementPage.vue`、`process-management.css`：五页共享紧凑工具栏；列表主日期、筛选、操作集中；低频档案操作置于“更多操作”；参数和填报字段统一160px，历史版本改下拉；诊断的匹配工况紧接工艺线；来源及统计说明折叠。保留真实错误、历史快照、示范、过期和缺失状态。
- `ProcessArchivePage.vue`、`DailyCollaborationPage.vue`：协作入口字段同宽、横向排列；参数筛选并入主工具栏，参数编辑区两行；不再强制操作占满独立一行。筛选不触发参数修改标记。
- `ManagementQualityPage.vue`：六个顶部评分/周期卡统一边界和间距；修复总分卡受通用article样式影响的留白；与下方表格间距8px。四维切换及规则标记不变。
- `lab-raw-records.css`：台账筛选同宽、区块间距8px；受控计算、原始记录内容及打印样式未改。
- `EvaluationResultsManager.vue`、`process-evaluation.css`：取消重复大标题/说明，新增评价任务与筛选合并；列表字段紧凑，不改任务和判定规则。
- `HighEfficiencySedimentationPage.vue`、`VFilterAnalysisPage.vue`、`ConfiguredUnitAnalysisPage.vue`与共用样式：八个单体页移除重复大页头；对象、周期、场景、完整度集中；模型版本、边界与重置入口移入“模型与说明”。业务诊断边界、必要DRAFT说明及五页签保留。
- `SafetyWorkspace.vue`：安全检查/隐患列表控件宽度与间距统一，职责说明可折叠；不修改权限与安全流程。

## 实际验证

- 管理端类型检查、生产构建通过；既有大包告警保留。42文件UI守卫及空白检查通过；工艺41项、协作7项回归通过。
- 默认本地入口5173：1440px诊断列表主控件同一行，日期框160px；五个工艺页在1280px均无整页横向溢出，表头距视口顶部约202—291px（含已有全局职责错误条）。打开历史日报、来源说明与版本可用，未替换用户本地档案。
- 协作入口5188：以既有试用工艺经理/运行工，通过正式接口在全新隔离测试库形成设计→目标→日数据提交确认→日报保存链。五个工艺页在1440/1280px均无整页横向溢出，匹配工况显示“常规工况（UI验收）”；1280px表头约189—270px。筛选后参数未被标为已修改，来源与历史可展开。
- 管理质量：四维3/4/3/8共18项均可访问，评分标准抽屉可打开；1440/1280px顶部六卡同排，表格前8px，无整页横向溢出。
- 提质增效：八个单体入口逐一点击，旧大页头均移除，1440px无整页横向溢出；曝气供气五页签和“模型与说明”展开/收起可用。未操作优化状态或重置样例。
- 化验：18类目录可见；pH台账和原始记录打开正常，受控结果仍为7.22，未保存/复核/更正业务数据。本轮未重新做全部18类打印验收。
- 过程评价：结果列表及筛选呈现已检查，历史任务仍可见；未新建、删除或锁定评价任务。
- 安全：独立环境以安全员身份查看检查任务/检查计划/模板库和隐患治理，职责说明可展开；1280px无整页横向溢出。本次未重新执行整改闭环，不代替已有安全业务验收。

## 环境和剩余限制

- 5173连接原8080，原数据保留；平台管理员已有“当前人员没有该步骤职责，不能代他人操作”提示仍可复现，未因UI精简隐藏。不能宣称全角色首页无错误。
- 历史工艺经理账号已从本任务历史找到；旧8085服务存在数据库连接失败，5187不能正常完成登录。本次未中断或重置旧服务。
- 为完成协作UI实测，复制现有测试启动器至临时目录，独立启动8086与5188；仅有隔离样例，不是恢复原试点数据。启动器/凭据不入仓库，临时密码不写文档。
- 尚未部署CDS、提交、推送或创建PR。前后端入口UI同步是本地代码与两套运行配置的验证，不代表线上同步或全站每个弹窗均验收完成。
