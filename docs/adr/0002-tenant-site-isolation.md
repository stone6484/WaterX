# ADR-0002：租户与厂区双层隔离

状态：已接受

所有业务表必须包含 `tenant_id`，厂级业务必须包含 `site_id`。复合外键同时带上 `tenant_id`，避免错误关联到其他租户的同名或同 ID 业务对象。

平台/区域用户通过 `user_role_scope` 获得跨厂权限。所有厂级接口由服务端验证 `X-Site-Id`，前端厂区选择器仅用于传递上下文，不承担安全控制。
