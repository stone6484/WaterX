# 后续批次验证：登录与指标详情

日期：2026-09-05。继首轮验收台之后，根据用户的自主推进授权与登录页请求继续。本轮仅本地改动，未提交、推送或部署。

## 已验证

| 范围 | 方法 / 结果 |
| --- | --- |
| 登录主产品接入 | 实际打开本地 `/`，显示新登录组件；原处理函数与 API 未修改；未使用真实密码或提交真实登录 |
| 登录 1440×900 | 独立预览 DOM 实测页面宽高 1440×900，品牌区宽约 822.85px、表单宽 350px |
| 登录 1280×720 | 紧凑处理后页面宽高与视口一致；默认尺寸下实看品牌、线稿、输入和按钮 |
| 登录 390×844 | 实际产品 DOM 实测无横溢出；表单 x=26、宽 338px，浅背景 Logo 与产品名保留 |
| 密码显隐 | 用非真实样例字符串验证密码类型切换；预览提交后清空密码，明确提示没有发送或验证 |
| 指标详情键盘 | Enter 打开；方向键切页签；Tab 末尾返回关闭按钮，Shift+Tab 反向返回末尾按钮；Esc 关闭后焦点回到原评分标准按钮 |
| 鼠标关闭 | 关闭按钮 / 遮罩可关闭；底层正文 overflow 恢复到打开前状态 |
| 真实详情版式 | 引入主产品全局 CSS 后检查：抽屉宽 680px、外层 padding=0、正文 12px、底部贴视口底边 |
| 数据与事实 | 打开生化池曝气指标，展示原有两条专业事实；没有复制或改写来源数据 |
| 发起改进 | 验收台接收到原模块事件并给出演示提示；没有创建业务记录 |
| 四维表格 | 合法合规 3、稳定达标 4、安全运行 3、经济高效 8 项；表头和表体列宽一一相等 |
| 窄容器 | 1040px 外容器、998px 可视表格、1140px 内容；修复后四维 18 项文字 / 按钮均无跨列，操作均为单行 |
| 控制台 | 登录页与验收台实测没有新增 warning / error |
| 静态守卫 | 捕获新工艺工作区未定义 `--wx-text-primary`，仅该声明改为已存在的 `--wx-n700`；其余业务逻辑未动 |
| 最终工程检查 | `check:ui` 通过，检查 34 个 Vue 文件、令牌与 16 个图标；类型检查通过；主产品生产构建通过（181 个模块），两份独立预览联合构建通过（64 个模块）；保留既有的大体积分块提示 |

收尾时账户工具显示主要额度已用 45%、剩余 55%；没有使用重置，停止扩展本轮任务以保留用户要求的 50% 余量。

## 验证边界

- 内置浏览器在切换显式视口后有截图缩放 / 裁切不一致现象。尺寸采用当前 DOM 回读，视觉以默认视口截图核对；不把受影响截图冒充精确尺寸的像素基线。
- 没有完整登录后的所有模块实测，也没有后端认证 / 权限回归；主产品登录改动只替换视图与绑定。
- 抽屉只读详情已验收；复杂编辑的未保存提示、嵌套模态与浏览器兼容矩阵未完成。
- 几何自检只覆盖当前样例、当前字号与容器，不证明未来数据、缩放或任意浏览器都不越界。
- 工作区同期存在其他工艺模块修改。`App.vue` 本轮仅处理登录导入及视图；其他工艺逻辑不归入 UI 交付成果。
