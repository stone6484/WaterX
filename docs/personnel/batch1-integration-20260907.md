# 日数据人员协作首批：主产品整合验收

日期：2026-09-07。范围：已交接的日数据协作首批；仅本地，不是全站权限或完整工艺后端交付。

## 接手范围

用户确认MVP围绕工艺管理必需功能推进，安全仅做必要优化；其他规划模块暂停。本批复用真实仓库中的V20、日数据服务、页面和测试，不复制同名设计目录中的代码，不改真实人员授权、生产库或部署配置。原5173/8080和独立5185/8083用途保持分开。

## 本轮必要修正

- `DailyCollaborationPage.vue`：提供未保存/进行中离开检查；刷新放弃需明确确认；关闭或刷新浏览器触发未保存提示。加载时准确显示正在读取，失败提供重试，不将网络失败说成没有授权。
- 同组件：切换页面、项目或卸载时使旧请求失效，清空上个上下文的说明、筛选和历史；迟到的成功或失败不回填旧内容。办理/来源/导出仍由原服务端检查权限，没有改成前端授权。
- `App.vue`：通过组件公开接口保护侧栏导航、退出登录和项目切换；非协作模式原页面保持原接入。后续人员任务添加的参数/日报保护也复用了该接口，未覆盖本批保护。
- `verify-collaboration.mjs`：使用实际Vue组件逻辑新增7项回归，不复制业务实现；没有新增依赖。

## 实际验证

| 项目 | 本轮结果 |
| --- | --- |
| 已交接后端测试集合 | 日数据10、安全工作流9、既有安全API1、令牌1、风险2、V19恢复1，共24项通过，0失败/错误/跳过。该集合不构成新增V21业务验收 |
| 既有工艺逻辑 | 40项通过；未修改计算公式、目标值或旧浏览器档案 |
| 接入保护 | 7项通过：加载保护、未保存0保留、显式放弃、跨项目迟到成功/失败隔离、读取重试、确认来源只读 |
| 构建与规范 | 常规管理端和独立协作模式构建通过，221模块；UI守卫41个Vue文件通过；原大分块警告保留。首次构建缺Node路径，复用已有运行时后通过，未改全局环境 |
| 真实页面 | 复用5185已登录的运行工甲会话，打开专用09-06任务，输入日进水量0；尝试切到诊断和退出均被拦截、0仍保留、项目选择禁用；保存后可以提交审核 |
| 独立账号接口 | 经理账号真实登录、创建专用任务和确认；审核前来源409，确认后R4/V1且0保留；厂长账号只读、导出403。未用前端切换姓名模拟授权 |
| 旧记录保留 | 原09-07记录仍R11/V2；没有覆盖它。本轮新增09-06验收记录，均为临时测试库，不是生产事实 |
| 视觉与日志 | 当前1512×722页面截图显示列表与主壳一致、无内容遮挡；末次运行数据页控制台warning/error为空。未复验所有移动设备和整站页面 |

本轮使用现有独立服务，未停止或重启任何已有后端。新建验收记录可保留供试用，不是正式档案。

## 与下一批并行工作的边界

联合验收期间，`人员角色和权限`任务新增V21、ProcessArchive与ProcessCalculator等文件。首次全后端运行31项中，新 `ProcessCalculatorTest.frozenRulesMatchExistingEngineAcross828Rows` 失败（expected 0 / actual 138），其余30项通过，已将具体结果同步该任务修正；本轮没有删除、跳过或弱化该测试，也没有擅改其模型。

上述24项为明确选取的首批及安全回归，不是把31项失败改报成功。首批构建通过之后，下一批又更新App入口，当前诊断/参数/日报可能显示其在途页面；本轮仅交付运行日数据协作，不把在途V21页面或旧8083后端作为已完成配套分析的证据。下一批修正及全量复验由其独立记录更新。

已两次同步双方文件边界：主产品保留首批页面接入保护，人员任务保留其参数/报告增量；不得同时覆盖同一文件。下一批确认交接后再统一回归，暂不继续其他规划模块。

## 试用及发布边界

- 本批入口：`http://127.0.0.1:5185/` → 工艺管理 → 运行数据填报。当前Chrome已有运行工测试会话；凭据沿用原安全临时机制，不写入本文。
- 设计、目标、诊断和日报的完整后端链路属于下一批，不因首批整合完成而宣称完成。
- 按用户最新要求，CDS备份故障排查暂缓；正式数据保护及恢复要求没有取消。
- 没有提交、推送、创建PR、合并或部署；没有升级原8080或线上数据库，未扩展其他规划模块。
