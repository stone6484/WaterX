# 工艺管理 MVP 本地验证 — 2026-09-06

## 范围

基于真实仓库现有半成品继续，保留同期UI与其他未提交修改。首版只收集人工日数据、维护项目参考/目标、计算偏差、保存日结果与日报；不开发原因诊断、操作指导、设备指令或自动接口。

## 修改文件

均位于 `apps/admin-web/src/modules/process-management/`：

- `daily-input.ts`：集中日累计、日均、当日记录等提示；空目标与示范空白补值策略。
- `entry-table.ts`：导出字段、表格文本安全、身份/单位/口径/状态校验，全部成功后才替换草稿。
- `MetricEditor.vue`：日口径提示；取消设计页重复参考列；切换控制方式不注入示范数字。
- `ProcessManagementPage.vue`：仅补空白、工况历史浏览及保存版本刷新、移除自动汇集入口、集中表格适配、页内完整日报预览。
- `adapter.ts`：移除未接入的自动汇集占位函数；其余持久化规则不变。
- `process-management.css`：完整日报预览及打印隔离、黑白紧凑样式。
- `verify.mjs`：32项原测试保留，新增8项，共40项。

另同步业务规则文档及根 `HANDOFF.md`。本轮未新增依赖，未修改App.vue/共享组件/后端。

## 工程验证

- `node apps/admin-web/src/modules/process-management/verify.mjs`：40/40通过，调用真实代码。
- 新增测试覆盖：日口径、空目标、示范保留自定义值与0/异常/不适用、已有控制方式/零容差、真实xlsx内存往返、限定符/负值保真、跨项目/日期/单位/口径/重复/公式文本/异常原因拦截、导入失败原子性、旧模板兼容。
- 管理端构建包含类型检查，通过；183个模块，已有入口体积警告保留。
- UI静态守卫通过。Git差异与新增文件空白检查通过。

## 浏览器实际行为

环境：Chrome，本地 `http://127.0.0.1:5173/`；第一污水处理厂（示例），一期生化线，业务日期2026-09-06。临时后端以原启动器恢复，已登录；不记录凭据。

1. 设计手工填入进水COD=321后补入示范设计值，321保留，设计保存V1。
2. 空工况仅修改COD控制方式为上限，目标仍空；填入0再补样例，0保留。随后明确改为验收目标280，启用工况“0906人工日数据验收工况”。修改说明后保存V2，页面显示V2；点V1可见旧说明为空，旧版本只读。
3. 日数据COD=0后补完整样例仍为0；随后明确改为300以验算偏差。水量显示日累计、浓度显示日均，保存日数据V1。
4. 从已保存源生成138项结果：设计321、目标≤280、实际300、偏差+7.1%；状态按项目示范容差判定，不是法定合规结论。整体110项有效、28项待配置；正常64、预警31、告警8、未判定28、另7项参考。
5. 保存日报V1；工况变化后日报仍保留旧快照并提示来源更新。重新更新保存V2须确认，列表仍一条；来源下钻显示日数据COD=300。
6. 搜索COD后切到完整日报预览，仍呈现全部138项，分析看板隐藏；无筛选误导。
7. 运行模板实际落盘68行，回读确认日期、单位、口径、实际值；日报实际落盘138行，回读确认V1快照、设计321/实际300和末项“异常设备”。下载事件等待未返回，但文件实际落盘已核实。
8. 模板导入文件选择返回权限拒绝，未绕过；页面导入/保存端到端仍未验证。纯适配层测试不能冒充浏览器验收。
9. 真实Chrome打印预览可打开，A4横版、黑白、连续表格和重复表头可见。修正旧样式字号/留白后，样例9页内容加1张空白尾页；尾页问题仍在，未保存PDF文件或发送到实体打印机，不标为归档打印全部通过。
10. 1440×900下文档宽1440，双看板均588px；无页面整体横向溢出。登录和本批页面控制台未发现新增警告或错误。

## 已知边界与下一步

- 本批可用于人工试用与业务校核，不是正式生产系统。数据仍在当前浏览器/域名的本地存储；后端只承担原认证与项目读取，未接入本批档案持久化。
- 打印空白尾页已做根容器分页修订，详见下方续验；仍须保存PDF逐页核查。在允许文件选择的环境完成模板导入验收。
- 28项未配置模型、项目参数校准和不可变指标ID迁移仍待后续，不自行扩大本批范围。
- 工况停用/修改后历史重算选择策略尚未完善；旧结果快照不被覆盖。
- 仅本地；无提交、推送、PR或CDS操作。

## 同日续验：打印根容器与导入权限

- 唯一新增业务代码改动是打印样式：html根、body与日报使用同一命名分页，消除匿名根尾片多余分页。同一Chrome样例打印页数从10页变为9页，样式已加载确认。
- 尚未保存最终PDF。打印对话框未能稳定保持，后续浏览器控制会话不可用；首尾内容、连续页行完整性仍需最终文件核查。上述页数变化不等于完整归档打印验收。
- 页面“导入模板”已点击，选择本地已导出的运行模板时工具返回 `Not allowed`。未绕过权限，未保存覆盖日记录；导入后确认及保存链仍待验收。
- 40项真实引擎/适配/存储检查再次通过；管理端类型检查和生产构建通过，183模块及原有大分块警告；UI静态守卫通过。首次构建受沙箱临时目录写入限制，授权重跑通过。
- 最终控制台复验未完成，不将旧轮控制台结果作为本轮新增证据。
